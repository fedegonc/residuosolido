package com.residuosolido.app.config;

/**
 * Intentionally left as a no-op to avoid duplicate locale beans.
 * InternationalizationConfig already defines localeResolver and
 * localeChangeInterceptor, and WebConfig registers the interceptor.
 */
@Deprecated
public class LocaleConfig {
}
