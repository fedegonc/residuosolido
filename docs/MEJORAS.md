# Superficies de Mejora — Estado

> **Propósito:** tabla centralizada de todas las mejoras posibles del
> sistema, con su estado actual: implementado, descartado o diferido.
>
> **Fecha:** post-commit (`15b7db5`)
> **Tests:** 181, 0 failures

---

## Leyenda

- **Implementado:** hecho y funcionando
- **Descartado:** evaluado y rechazado (con justificación)
- **Diferido:** pendiente, postergado a después de la defensa
- **Latente:** código existe pero sin UI activa
- **Retirado:** se implementó, después se decidió sacarlo (con justificación) — distinto de Descartado, que nunca se llegó a implementar

---

## Tabla de superficies

| # | Mejora | Estado | Justificación |
|---|---|---|---|
| 1 | Auth con roles + onboarding forzado | Implementado | Spring Security 6, roles USER/ORGANIZATION |
| 2 | Solicitudes de recolección (CRUD) | Implementado | Crear, editar, eliminar, ver detalle |
| 3 | Rastreo por teléfono + código privado | Implementado | 8 caracteres, sin login requerido |
| 4 | Selector de código de país (UY/BR) | Implementado | Normalización E.164 |
| 5 | Flujo de estados (PENDING→IN_PROGRESS→COMPLETED/REJECTED) | Implementado | Con optimistic locking |
| 6 | Kanban integrado al dashboard de org | Implementado | 4 columnas, sin página aparte |
| 7 | Blog estático (3 artículos) | **Descartado** | Eliminado del MVP; sin BlogController ni templates |
| 8 | Métricas públicas por ciudad | Implementado | Sin auth requerido |
| 9 | Rate limiting de invitados | Implementado | Ventana deslizante por IP |
| 10 | Bloqueo por intentos de login | Implementado | 5 intentos, 15 min bloqueo |
| 11 | PWA instalable | **Descartado → retomado en #143** | manifest.json, SW, install prompt — luego removidos; solo queda un Service Worker "kill-switch" para desinstalar el viejo en navegadores que lo tenían |
| 12 | i18n español/portugués | Implementado | Client-side + server-side |
| 13 | Dark mode toggle | Implementado | CSS variables, localStorage, prefers-color-scheme |
| 14 | FOUC fix (flash de texto sin traducir) | Implementado | visibility:hidden hasta i18n ready |
| 15 | Diseño canónico (variables CSS, BEM) | Implementado | 0 estilos inline, 0 hex fuera de :root |
| 16 | Empty states con iconos + microcopy | Implementado | Font Awesome, sin emojis |
| 17 | Microcopy anti-slop | Implementado | Descripciones concretas, no aspiracional |
| 18 | CSRF en todos los formularios | Implementado | Spring Security |
| 19 | DTO RegistrationForm (sin mass-assignment) | Implementado | Validación server-side |
| 20 | Optimistic locking (@Version) | Implementado | En transiciones y borrado |
| 21 | Validación de imagen antes de persistir | Implementado | Tipo, extensión, tamaño |
| 22 | Contraseña mínima 3 caracteres | Implementado | Decisión de fase MVP |
| 23 | Service Worker cache (CSS/JS only) | **Descartado** (ver #100) | HTML network-first, sin pre-cache — el SW completo se descartó después junto con el resto de la PWA (#11) |
| 24 | Limpieza de claves muertas (messages_*) | Implementado | 136→91 claves, ES/PT sincronizados |
| 25 | Documentación centralizada con índice docs/INDICE.md | Implementado | Single source of truth |
| 26 | Diagramas UML (casos de uso, ER, clases, estados) | Implementado | draw.io, 4 figuras |
| 27 | 181 tests (unit + integration + e2e) | Implementado | 0 failures |
| 28 | Metodología iterativo-incremental (4 fases) | Implementado | docs/METODOLOGIA.md |
| 29 | 21 tradeoffs documentados | Implementado | docs/DEFENSA.md |
| 30 | Deploy en Render.com (PaaS) | Implementado | GitHub→deploy automático |
| 31 | Páginas públicas /documentos y /diagramas | Implementado | DocsController, índice de docs y figuras |
| 32 | Diagrama de secuencia UML 2.5 (Figura 4) | Implementado | figura4-secuencia.drawio, flujo de creación de solicitud |
| 33 | CI/CD pipeline (GitHub Actions) | Descartado | Workflow eliminado; la verificación es manual y Render construye el artefacto sin ejecutar tests dependientes del entorno |
| 34 | **AWS (IaaS)** | **Descartado** | Complejidad no aporta a la tesis. Ver docs/TRADEOFFS.md §15 |
| 35 | **VPN para acceso** | **Descartado** | Render.com da dominio público + HTTPS |
| 36 | **MCP de AWS** | **Descartado** | Sin credenciales AWS, sin valor para MVP |
| 37 | **Panel Admin / moderación** | **Descartado** | Fuera de scope MVP. Ver docs/TRADEOFFS.md §2 |
| 38 | **CMS para blog** | **Descartado** | Blog estático suficiente. Ver docs/TRADEOFFS.md §5 |
| 39 | **Mapas / geolocalización** | **Descartado** | Fuera de scope. Ver docs/TRADEOFFS.md §10 |
| 40 | **Mascota virtual** | **Descartado** | Microcopy + iconos en su lugar |
| 41 | **Emojis en UI** | **Descartado** | Inapropiado para tesis académica |
| 42 | **Separar Cuenta de Organización** | **Descartado** | Mono-modelo User. Ver docs/TRADEOFFS.md §1 |
| 43 | **MongoDB réplica set / transacciones** | **Descartado** | Standalone suficiente para MVP. Ver docs/TRADEOFFS.md §3 |
| 44 | **WhatsApp Business API real** | **Descartado** | Eliminadas del MVP. Ver docs/TRADEOFFS.md §8 |
| 45 | **Cloud storage (S3/Cloudinary)** | **Descartado** | Local en disco. Ver docs/TRADEOFFS.md §9 |
| 46 | Fusionar request-form + request-edit | Implementado | `RequestEditController` fusionado en `RequestController` |
| 47 | Fusionar perfiles (user + org + onboarding) | Implementado | `OrgOnboardingController` fusionado en `OrgProfileController` |
| 48 | Fusionar dashboards (user + org) | Diferido | Riesgo medio. Ver docs/TRADEOFFS.md §14 |
| 49 | Reducir clases CSS con utilities | Diferido | 292 clases, cambio masivo. Ver docs/TRADEOFFS.md §14 |
| 50 | Consolidar stat-card (20→8 clases) | Diferido | Posible sobre-diseño |
| 51 | Simplificar kanban (22→15 clases) | Diferido | Componente más complejo |
| 52 | Unificar fuentes de copies (3→1) | Diferido | data-i18n + messages_* + JSON |
| 53 | Catadores CRUD (InformalCollector) | Descartado | Planificado pero nunca implementado en este estado; subsistema eliminado por completo (ver nota más abajo) |
| 54 | Contraseña mínima 8+ (producción) | Diferido | 3 chars para MVP. Ver docs/TRADEOFFS.md §4 |
| 55 | Asignación de recolector a solicitud (RF-8) | Diferido | Catadores latentes |
| 56 | Transiciones suaves entre temas | Diferido | Intencional: evitar parpadeo |
| 57 | SEO / meta tags dinámicos | Diferido | El sistema es una app, no un blog |
| 58 | Tests E2E con Selenium/Playwright | Diferido | E2E con MockMvc suficiente |
| 59 | Monitoreo (Prometheus/Grafana) | Diferido | Fuera de scope MVP |
| 60 | Logs estructurados (JSON) | Diferido | Logs Spring Boot suficientes |
| 61 | Rate limiting con Redis | Diferido | In-memory suficiente para MVP. **Tripwire:** antes de escalar a más de 1 instancia del servidor — el estado in-memory no se comparte entre instancias, el rate limit dejaría de funcionar en silencio, no con un error visible |
| 62 | Multi-tenancy (multi-frontera) | Diferido | Una sola frontera (Rivera-Livramento) |
| 63 | Upgrade Java 17→21 (pom + Dockerfile) | Implementado | Java 21 LTS en pom, Dockerfile y CI |
| 64 | Upgrade JaCoCo 0.8.11→0.8.15 | Implementado | Patch, sin breaking changes |
| 65 | Upgrade maven-pmd-plugin 3.21→3.28 | Implementado | PMD 7, rulesets category-based |
| 66 | Upgrade FontAwesome 6.5→7.2 | Implementado | CDN y webjar actualizados |
| 67 | Migración Spring Boot 3.2→3.5 | Diferido | Paso intermedio seguro antes de 4.0 |
| 68 | Migración Spring Boot 3.5→4.0 | Diferido | 115 breaking changes, @MockBean eliminado |
|| 69 | Validación inline de `email` y `name` en `User` | Implementado | Validación server-side sin value objects separados |
|| 70 | Notificaciones WhatsApp / asincrónicas | Descartado | Eliminadas del MVP; no proveedor configurado |
|| 71 | OpenAPI / Swagger UI | Implementado | `/swagger-ui.html` generado desde controllers |
|| 72 | Centralización de rutas en `Routes.java` | Implementado | Única fuente de verdad para endpoints y seguridad |
|| 73 | Layouts anidados (`layout/base-sidebar`) | Implementado | Elimina duplicación de `.org-layout` en dashboard/requests/profile. Ver `docs/DIAGRAMAS.md` §Sistema de Layouts |
|| 74 | Componentes UI reutilizables (`fragments/ui/`) | Implementado | button, alert, card — DRY para markup repetido |
|| 75 | HTMX states CSS (`htmx-states.css`) | Implementado → eliminado en #139 | Retroalimentación visual (spinner, opacity) sin JS extra |
|| 76 | Fix `.how__grid` 3→4 columnas en landing | Implementado | Bug: 4 pasos ("Solicitás/Asignamos/Retiran/Impactamos") forzados en grilla de 3, el 4to quedaba descolgado en desktop |
|| 77 | Spinner en botón "Mis solicitudes" (navbar, invitado) | Implementado → eliminado en #139 | `.htmx-indicator` en vez de solo opacity al abrir el modal de rastreo |
|| 78 | Toast de error genérico en fallos HTMX | Implementado → reemplazado en #139 | `htmx:responseError`/`htmx:sendError` → alerta reutilizando `_server_error_generic` (i18n) |
|| 79 | Eliminar `fragments/ui/` (librería de componentes duplicada, sin uso) | Implementado | `button.html`/`card.html`/`alert.html` no eran referenciados por ningún template real |
|| 80 | Unificar naming BEM del footer (`.site-footer`+`.footer-inner`+`.footer-min__*` → `.footer`/`.footer__*`) | Implementado | 3 prefijos de bloque distintos para un solo componente |
|| 81 | Eliminar los 2 últimos estilos inline (`error/404.html`, `users/requests.html`) | Implementado | `MEJORAS.md` #15 afirmaba "0 estilos inline"; ahora es cierto |
|| 82 | Tokenizar escala tipográfica (`--text-2xs`…`--text-3xl`) | Implementado | 9 valores de `font-size` estaban hardcodeados y repetidos en todo `app.css`; ahora las 57 declaraciones referencian un solo token cada una |
|| 83 | Consolidar breakpoints a 3 valores fijos (640/768/1024) | Implementado | Había 4 valores ad-hoc (640/700/768/900) sin escala; `var()` no funciona dentro de `@media`, así que se documentó como comentario fijo en vez de token CSS |
|| 84 | Tokenizar colores de badges de estado (`--badge-*-bg/-text`) | Implementado | Antes hex crudo repetido; se mantuvieron tonos propios (más saturados que los `alert--*`) por decisión de diseño — no se fusionaron con los tokens de alert |
|| 85 | Tokenizar transiciones (`--transition-fast/-base`, `--ease-base/-in/-out`) | Implementado | 9 transiciones sin ningún token; 5 usaban `transition:all` (recalcula todas las props animables). Ahora cada regla anima solo la(s) propiedad(es) que realmente cambia(n) — `background-color`/`border-color`/`color`/`opacity` — con duración y easing tokenizados |
|| 86 | Instalar skill `emil-design-eng` (design-engineering, animación/craft) en `.agents/skills/` | Implementado | Skill externa (github.com/emilkowalski/skills) para auditar futuras decisiones de animación con el framework de Emil Kowalski (Sonner/Vaul) |
|| 87 | Fix `ease-in` → `ease-out` en `.htmx-settling` + eliminar token `--ease-in` | Implementado (la parte HTMX murió en #139; la limpieza del token sigue) | La skill: "Never use ease-in for UI animations" — se detectó que el contenido entrante usaba la curva que la skill prohíbe |
|| 88 | Feedback de presión `:active{scale(.97)}` en `.btn`, `.check-card`, `.radio-card` | Implementado | Principio de la skill: "Buttons must feel responsive to press" — no existía ningún estado de presión en el sistema |
|| 89 | Brief de personalidad visual (anti-slop) | Implementado | `docs/TRADEOFFS.md` §22 — 3 adjetivos (territorial/institucional/directo al grano) para evaluar decisiones visuales futuras |
|| 90 | Eliminar gradientes diagonales (`.btn--primary`, `.navbar__logo`, `.navbar .btn--primary`, `.hero`) | Implementado | Tell visual más repetido de templates genéricos de IA; color plano + hover real en su lugar (antes `.btn--primary` no tenía ningún estado hover) |
|| 91 | Formalizar `--accent`/`--accent-hover` como tokens reales en `:root` | Implementado | Se usaba en `modal__option-icon--accent` pero nunca estaba definido (caía a un hex huérfano `#e76f51`); ahora es un terracota sobrio (`#c1552c`) coherente con el brief |
|| 92 | Rediseñar "Cómo funciona" (landing): círculos numerados → numeración tipográfica editorial | Implementado | Los círculos con número + ícono son el segundo cliché más repetido de landings genéricas; ahora es número grande + borde superior, alineado a la izquierda |
|| 93 | Documentar y ajustar la escala tipográfica a un ratio real (1.125, Segunda Mayor) | Implementado | 6 de 9 pasos ya coincidían con el ratio dentro de 1px; se ajustó `--text-2xl` (24→23px) y se documentó `--text-3xl` como excepción deliberada ("display size") en vez de dejarlo como número suelto |
|| 94 | `addResources=true` en `spring-boot-maven-plugin` | Implementado | Los cambios en `static/`/`templates/` no se reflejaban en `mvn spring-boot:run` sin recompilar manualmente (`target/classes` quedaba desactualizado); ahora el classpath usa `src/main/resources` directo en modo dev |
|| 95 | Fusionar 5 cards en 1 con `.divider` — detalle de solicitud (`org/requests.html`) | Implementado | Contacto/Dirección/Materiales/Foto/Franja eran 5-6 boxes con borde+sombra propios para una sola unidad de información; ahora es 1 card con secciones internas separadas por `.divider`. La sección de acción (Aceptar/Finalizar) queda como card separada a propósito — es funcionalmente distinta |
|| 96 | Quitar cards anidadas en `request-form.html` (`.card`→`.form-section`) | Implementado | Las secciones "contacto"/"detalles" eran cards blancas con borde+sombra propios *dentro* de otra card blanca — blanco sobre blanco sin aportar jerarquía. Ahora son secciones planas; la única superficie elevada es la card exterior del formulario |
|| 97 | `--ease-out` custom (`cubic-bezier(.23,1,.32,1)`) en vez de la curva nativa | Implementado | La skill `emil-design-eng`: "built-in CSS easings... lack the punch". Afecta `:active` de botones/cards y el fade de HTMX — ahora se siente crisp, coherente con el brief institucional en vez de la curva nativa "floja" |
|| 98 | Entrada animada del modal de rastreo (`@starting-style`, fade + `scale(.96)`) | Implementado | El modal aparecía sin ninguna transición (pop instantáneo); ahora fade-in + scale sutil con `--transition-base`/`--ease-out`. Sin JS, CSS nativo moderno; degrada a aparición instantánea en navegadores sin soporte |
|| 99 | Fix asset corrupto: `plus-jakarta-sans-400.woff2` era HTML, no la fuente | Implementado | El archivo en disco (269KB) era una página de GitHub guardada por error en vez del binario. Reemplazado por el woff2 real (27KB, subset latin, verificado con magic bytes `wOF2`) — rompía la carga de la tipografía en todo el sitio |
|| 100 | Kill-switch para Service Worker huérfano (`/sw.js` + `SecurityConfig` permitAll) | Implementado | El PWA se descartó (#11) pero navegadores que lo visitaron antes del 15/9 seguían con el SW viejo activo, interceptando *todos* los fetches (rompía fuentes, HTML, todo) porque `/sw.js` devolvía 302→login. Nuevo `/sw.js` se auto-desregistra y borra caché en la próxima visita de cualquier navegador afectado |
|| 101 | Fondo de página blanco en vez de gris (`body{background:var(--surface)}`) | Implementado | `--bg` (gris) hacía doble función: fondo de página y relleno de hover/disabled/kanban. Se dejó `--bg` intacto para esos 6 usos funcionales y solo se cambió el body a `--surface` (blanco) — las cards siguen distinguiéndose por borde+sombra |
|| 102 | Métricas de LOC + diagnóstico de escala (¿estudiante o equipo?) | Implementado | `docs/TRADEOFFS.md` §23 — LOC por capa, estructura de las 48 clases backend, 428 commits/1 autor/14 meses, y argumento de por qué la escala es consistente con trabajo individual sostenido |
|| 103 | Permitir foto/peso/volumen/franja horaria en la **creación** de la solicitud (no solo al editar) | Diferido | Hoy esos 4 campos son `th:if="${isEdit}"` — el ciudadano debe crear y después editar para adjuntar una foto. No son campos sensibles, es fricción evitable |
|| 104 | Paginación en `org/requests.html` (lista) | Diferido | Sin `Pageable`/límite — renderiza todas las solicitudes que matchean el filtro de estado en una sola página. No escala con volumen alto |
|| 105 | Contrato explícito `User.getAcceptedMaterialsCsv()` en vez de `List.toString()` | Implementado | El filtro de materiales dependía del formato default de `toString()` de `List` (`"[A, B]"`) parseado con un regex que pelaba corchetes. Ahora hay un método propio, documentado, que es la única fuente de verdad del formato — el JS quedó más simple (ya no necesita el regex) |
|| 106 | Separar `app.js` — lógica exclusiva de página fuera del archivo global | Implementado | `filterMaterialsByOrg` (solo `request-form.html`) y el toggle view/edit (solo `org/profile.html`) pasaron a `request-form.js`/`org-profile.js`, cargados vía `layout:fragment="pageScripts"` (existía sin uso). `app.js` quedó con un comentario-manifiesto de qué componente usa qué página, para lo que sigue siendo compartido |
|| 107 | Fix: error de validación (`IllegalArgumentException`) redirigía a `/auth/login` en vez de al formulario de origen | Implementado | `GlobalExceptionHandler.handleIllegalArgument` usaba un target "genérico" por rol que, para un usuario no autenticado, siempre caía en `/auth/login` — un error de teléfono mal formado en `request-form.html` terminaba mostrándose en la pantalla de login (sin campo de teléfono). Ahora usa el header `Referer` para volver a la página de origen; verificado con `curl` simulando el POST |
|| 108 | Eliminar `RoleBasedLoginTargetUrlResolver` (`@Component` + DI) → `Routes.resolveHomeForRole()` (static) | Implementado | Era una función pura (`Authentication → String`) envuelta en un bean inyectado en 4 archivos de 2 paquetes distintos, duplicando además los strings `"/acopio/inicio"`/`"/usuarios/inicio"` que ya existían como `Routes.ORG_HOME`/`Routes.USER_HOME`. Ahora es un método estático en `Routes.java` (la fuente única de verdad para rutas) — 1 archivo menos, sin duplicar constantes. Tests migrados a `RoutesTest.java` (6/6 verde) |
|| 109 | Documentar la truth table de `request-form.html` | Implementado | Comentario HTML al inicio del archivo con las 3 flags (`isEdit`×`isGuest`×`needsPhone`) y sus combinaciones reales — antes había que simular 8 casos mentalmente leyendo los `th:if` sueltos |
|| 110 | `viewType` de `String` crudo a enum `RequestViewType` | Implementado | `"list"`/`"detail"` eran strings sin ninguna garantía de compilación — un typo no se detecta hasta runtime. Ahora `RequestViewType.LIST/DETAIL`, comparado en el template con `T(...)` (mismo patrón que `RequestStatus` en otros templates). Tests de `EndToEndFlowsTest` actualizados |
|| 111 | Extraer botón "Mis solicitudes" duplicado del navbar a fragments | Implementado | Estaba escrito 2 veces, idéntico, para desktop/mobile. Se probó parametrizarlo por clase vía `th:replace` pero el `--` de BEM dentro de un string literal rompe el parser de expresiones de Thymeleaf/attoparser (`TemplateProcessingException`) — se resolvió con 2 fragments sin parámetro (`trackButtonDesktop`/`trackButtonMobile`), adyacentes en el archivo en vez de duplicados a 40 líneas de distancia |
|| 112 | Corregir `docs/DIAGRAMAS.md` — citaba archivos JS ya eliminados | Implementado | "Decisiones de Arquitectura" mencionaba `fragments/toggle-view-edit.html`/`request-form-js.html`, borrados al principio de esta sesión (ver #106) |
|| 113 | **Fix crítico**: `org/profile.html` tiraba 500 siempre — `organization.hasCity()` no existía | Implementado | Bug preexistente desde el commit `6aba626` ("podado de backend"), descubierto al correr `EndToEndFlowsTest` completo. Agregado `User.hasCity()` (mismo patrón que `hasPhone()`) |
|| 114 | **Fix crítico**: `org/dashboard.html` (Kanban) tiraba 500 siempre — `T(...)` no se puede usar dentro de argumentos de un fragment call | Implementado | Thymeleaf lo prohíbe por seguridad ("Instantiation of new objects... forbidden"). Se resolvió con `th:with` para resolver los 4 `RequestStatus` antes de pasarlos como variables simples a `kanban-column :: column(...)` |
|| 115 | **Fix crítico**: `fragments/kanban-column.html` tenía una llave `}` sobrante en un ternario anidado + NPE si `requests` llegaba null | Implementado | Bug de sintaxis preexistente (3 aperturas `\${` vs 4 cierres `}`, verificado con conteo exacto) que quedaba enmascarado por el bug #114 — al arreglar uno, apareció el otro. `th:class` del ícono de estado corregido; `requests`/`isEmpty()` protegidos con `safeRequests=\${requests != null ? requests : {}}` (nota: `T(...).metodo()` tampoco se permite en este contexto, solo acceso a campos estáticos — se usó el literal SpEL `{}` en vez de `Collections.emptyList()`) |
|| 116 | Fix título duplicado en la pestaña de la home ("Eco Solicitud — Eco Solicitud") | Implementado | `index.html` tenía `<title>Eco Solicitud</title>` — igual al `$LAYOUT_TITLE`, duplicando el patrón `$LAYOUT_TITLE — $CONTENT_TITLE`. Cambiado a `<title>Inicio</title>` |
|| 117 | Fix `favicon.svg` — todavía tenía el gradiente de la Fase 1 | Implementado | El resto del sitio ya es plano (sin gradientes) desde la Fase 1, pero el ícono de la pestaña se escapó del barrido — tenía el mismo verde degradado `#52b788→#1b4332`. Ahora plano (`--primary` #2d6a4f) |
|| 118 | Extraer pieza `StatTile` (`fragments/stat-tile.html`) | Implementado | El bloque ícono+valor+label estaba escrito 7 veces idéntico (3 en `org/dashboard.html`, 4 en `users/requests.html`). Ahora es 1 fragment parametrizado (`icon,value,labelKey,labelText`), verificado en vivo con login real en ambos roles |
|| 119 | Extraer pieza `PageHeader.greeting` (`fragments/page-header.html`) | Implementado | El bloque "Hola, {nombre}" + subtítulo se repetía en `org/dashboard.html` y `users/requests.html` (con una diferencia real: uno lleva "¡...!" y el otro no — parametrizado con `exclamation:boolean`). Las acciones de cada header quedaron fuera del fragment porque sí difieren genuinamente por página |
|| 120 | Fusionar 8 fragments chicos en `fragments/ui.html` | Implementado | `empty-state`, `badge`, `info-row`, `stat-tile`, `org-options`, `alert`, `breadcrumb`, `page-header` eran 8 archivos de 8-11 líneas cada uno, cada uno repitiendo el mismo boilerplate `<html>`/`<body>`. Fusionados en 1 archivo con 8 `th:fragment` — bajó de 14 a 7 archivos en `fragments/` y de 330 a 309 líneas totales. Actualizadas las 7 plantillas + 1 controller (`RequestCreateController`) que los referenciaban; verificado en vivo (home, dashboard, perfil, lista/detalle, track, org-options) |
|| 121 | Ritmo de espaciado interno/externo (`--space-lg`, `.mb-lg`/`.mt-lg`) | Implementado | Todo el espaciado entre bloques usaba `--gap` (1rem) parejo, sin distinguir "dentro de una sección" de "entre secciones". Nuevo token `--space-lg` (2.5rem) para separación entre secciones distintas — aplicado en `index.html` (hero→pasos→CTA), `org/dashboard.html` y `users/requests.html` (header→stats→contenido). El espaciado interno (`--gap`/`.mb`) se mantiene igual dentro de cada sección |
|| 122 | Agrupar CSS compartido `.navbar`/`.footer` | Implementado | `.navbar__inner`/`.footer__inner` eran casi idénticos (mismas 6 propiedades, navbar solo agrega 2), y `.navbar`/`.footer`, `.navbar__brand`/`.footer__brand`, `.navbar__links`/`.footer__links` repetían el mismo patrón `display:flex;align-items:center...`. Agrupados en selectores compartidos (`.navbar,.footer{...}`) con las diferencias reales (tamaño, gap, bordes) en reglas separadas — mismo resultado visual, sin propiedades duplicadas. Cero cambio de HTML, solo CSS |
|| 123 | Registro simplificado: nombre + teléfono + PIN de 4 dígitos | Implementado | Fricción mínima para pruebas (ver `docs/TRADEOFFS.md` §24 para el tradeoff completo). `RegistrationForm`/`UserRegistrationService` cambiaron email→teléfono y contraseña 8+→PIN 4 dígitos; `username` ya acepta espacios (es "nombre" ahora). Tests migrados (`UserServiceTest` 17/17, `MvpRegressionTest` 9/9, `TransversalBrowserTest`). Verificado en vivo: 2 registros + login reales de punta a punta |
|| 124 | `MongoIndexMigration` — reemplaza `auto-index-creation` por migración explícita | Implementado | El índice único de `email` (sin `sparse`) chocó al dejar de pedir email en el registro (2+ usuarios con `email=null`) y tiró abajo el arranque completo (`IndexKeySpecsConflict`). Se desactivó `spring.data.mongodb.auto-index-creation` y se agregó un `CommandLineRunner` idempotente que arregla el índice viejo y asegura los otros 3 `@Indexed` del proyecto explícitamente — más robusto que la creación automática, y portable a una base nueva |
|| 125 | Fix inconsistencia: teléfono de registro usaba un input suelto en vez del selector binacional | Implementado | El campo de teléfono del registro (#123) se implementó con un `<input>` simple, distinto del selector país+nacional+DDD ya usado en `request-form.html`/`org/profile.html`. Ahora `register.html` usa exactamente el mismo componente (prefijo `phone`, ya soportado por `app.js` sin cambios), y `RegistrationForm` construye el teléfono vía `PhoneNumber.normalize(countryCode, phoneNational, ddd)` en vez de un string crudo. Verificado en vivo con ambas ramas (UY y BR+DDD) |
|| 126 | Crear `docs/BOILERPLATE_VS_CORE.md` | Implementado | Documento vivo (v1) que clasifica cada clase backend/frontend/infra en Boilerplate, Core o Zona gris — separa qué es infraestructura genérica de Spring Boot de qué es el aporte real del dominio. Referenciado desde `docs/INDICE.md` |
|| 127 | **TEMPORAL** — links a `MEJORAS.md`/`DEFENSA.md`/`BOILERPLATE_VS_CORE.md` + visor de diagramas UML en el footer | Implementado (sacar antes de producción) | Agregados para revisar avance fácil esta semana de desarrollo, vía `DocsController` (`/docs/{file}.md`, ambos ya públicos). **Tripwire: sacar todo esto antes de que un usuario real (no de prueba) vea el sitio** — no tiene sentido exponer docs internos de tesis en el footer de producción |
|| 128 | Visor embebido de diagramas UML (`/docs/diagramas`) | Implementado | El link de diagramas del footer (#127) originalmente apuntaba a los 5 `.drawio` crudos (XML sin renderizar, requería copiar/pegar a app.diagrams.net a mano). Ahora `DocsController.viewDiagrams()` lee cada `.drawio` de `docs/diagrams/` y lo embebe inline en un `data-mxgraph` (JSON con la clave `xml`, escapado automáticamente por Thymeleaf al atributo HTML), renderizado client-side por el script oficial `viewer-static.min.js` de draw.io. Evita el problema de CORS de la alternativa (`?lightbox=1#U<url>`, que depende de que draw.io pueda hacer fetch cross-origin a `localhost`) porque el XML ya viaja embebido en el HTML, sin fetch externo. CSP `script-src` ampliado con `https://viewer.diagrams.net`. Mismo tripwire que #127 |
|| 129 | Centralizar secretos: `.env` + `.env.example`, sacar credencial hardcodeada de `application-dev.properties` | Implementado | `application-dev.properties` tenía la connection string de MongoDB Atlas (usuario+password reales) hardcodeada y committeada en git — credencial rotada en Atlas tras detectar el leak. Ahora `application-dev.properties` no tiene `spring.data.mongodb.uri` propio, hereda el de `application.properties` (`${SPRING_DATA_MONGODB_URI}`), que Spring Boot carga desde `.env` en la raíz (gitignored) vía `spring.config.import=optional:file:.env[.properties]`. `.env.example` (versionado, sin secretos reales) documenta las 3 claves (`SPRING_DATA_MONGODB_URI`, `MONGODB_DATABASE`, `UPLOAD_DIR`). En Render, las mismas claves van como variables de entorno del servicio. Se borró `.env.local` suelto (no lo leía nadie, tenía la credencial vieja ya revocada). Ver `docs/TRADEOFFS.md` §26 |
|| 130 | Consolidar pantallas de organización: 4 → 2 (dashboard Kanban + onboarding absorbidos) | Implementado | `org/dashboard.html` + `fragments/kanban-column.html` + `OrgDashboardController` eliminados — el Kanban duplicaba `org/requests.html` (misma data, mismas transiciones accept/reject/complete vía detalle; el filtro por estado hace el mismo trabajo) y cargaba `pendingRequestsList` que nunca se renderizaba. Las stat tiles pasaron al header de `org/requests.html`. `org/complete-profile.html` + sus endpoints eliminados — era un subconjunto de `org/profile.html` (solo phone+city): ahora el perfil abre en modo edición cuando `!isProfileComplete()` y `UserService.updateProfile` auto-completa el flag si la org tiene phone+city (método `completeOrgProfile` eliminado). El check `needsProfileCompletion()` se movió a `OrgRequestController`. `/acopio/inicio` y `/acopio/completar-perfil` dejaron de existir; `resolveHomeForRole` ahora devuelve `/acopio/requests` para orgs. Sidebar org quedó con 2 links. También eliminados de `RequestService`: `getRecentPendingRequestsByOrganization`, `getRequestsByOrganizationGroupedByStatus` (huérfanos). Tests migrados: `EndToEndFlowsTest`, `RoutesTest`, `CriticalSecurityTest`, `OrganizationControllerTest`, `UserServiceTest`, `OrganizationBrowserTest`, `TransversalBrowserTest` |
| 135 | `/solicitar` adopta el patrón split (panel blanco + navy) de index/login/registro | Implementado | La página del formulario era la única de las 4 pantallas de acción que seguía siendo una `.card` flotante. Ahora usa `.split` → `.hero` (form) + `.hero--dark` con escape hatch contextual: invitado → `/rastrear`, logueado → `/mis-solicitudes`. El `grid grid--2` interno (contacto|detalles) se apiló — en el panel 3fr las 2 columnas quedaban en ~17rem. `.card--form` quedó huérfana y se eliminó del CSS. +4 claves i18n ×2 idiomas (`req_form_alt_*`) |
| 136 | Normalizar prefill de teléfono del index → `/solicitar` | Implementado | El index manda `?telefono=<texto libre>` y el server lo volcaba crudo en el input nacional de `request-form.html` con el select clavado en +598: `59899123456` (sin `+`) y cualquier número BR explotaban en `error.phone.invalid_length`/`invalid`. Nueva función `distributePrefill()` en el componente de teléfono de `app.js`: al cargar, si el valor trae código de país (con o sin `+`), lo reparte entre select / nacional / DDD — espejo client-side de `PhoneNumber.normalize`. Bonus: un input de 11 dígitos sin código jamás es UY válido (exige 8), así que se asume BR con DDD embebido. De yapa repara el mismo bug latente en `org/profile.html`, que bindeaba el E.164 completo al input nacional |
| 137 | Raíz única de formularios: `fragments/forms.html` (text/phone/check/checkGrid/pin/submit) | Implementado | Cada campo de formulario divergía por página: el teléfono existía en 4 versiones (index crudo, track E.164 manual, request-form y register/profile con componente). Nuevo archivo canónico con 6 fragments: `text(id,name,type,label,placeholder,required,value)`, `phone(prefix,...)` (deriva ids `{prefix}CountryCode/National/DddGroup` y names `{base}CountryCode/{prefix}National/{base}Ddd` donde base=prefix−'phone'), `check`, `checkGrid(items,name,selected,i18nPrefix)` (absorbe el `.check-grid` duplicado de request-form y org/profile), `pin` (ex `password-field.html`, eliminado) y `submit(icon,label,variant)`. Migrados index, login, register, track, request-form, org/profile. Consecuencias: index ahora envía params estructurados (`guestCountryCode/guestPhoneNational/guestDdd`) que `request-form` bindea vía `param.*` (fallback `?telefono=` + `distributePrefill` para links viejos); track usa el componente con `trackPhone` + hidden `#telefono` que app.js compone a E.164 al submit (arregla el 500 latente de `PhoneNumber.normalize` con formato nacional). Fix colateral: `data-i18n` en `<label>` hacía `textContent` y borraba el `<span class="req">*</span>` — ahora el texto i18n va en span interno (también corregido en labels de ciudad/materiales) + guard contra `data-i18n-attr` vacío en `applyTranslations`. `track-modal.html` absorbido en `track.html` como `:: modal` con wrapper `th:if="false"` (la extracción por nombre ignora ancestros); `.hero--dark .field label` nuevo para el DDD en fondo navy. Excepciones conscientes: `username` de login (único `autofocus`) y `codigo` de track (único `autocomplete="off"`) quedan inline. Verificado end-to-end con Playwright real: 13/13 checks (index→/solicitar UY+BR, track E.164, legacy `?telefono=`). -2 archivos, claves i18n muertas `split_guest_phone`/`track_phone_placeholder` eliminadas de es/pt |
| 138 | Eliminar contratos implícitos del frontend (modal inline, names explícitos, track sin hidden) | Implementado | Segunda pasada sobre #137: los patterns más crípticos eran *contratos invisibles* entre markup/JS/server. (a) **Modal de rastreo inline**: el fragment `users/track :: modal` + `th:if="false"` + branch `HX-Request` del controller + fetch HTMX del navbar servían para traer markup estático sin modelo — ahora el modal vive inline en `#trackModal` del navbar (`is-hidden`, solo `!isAuthenticated()`), se abre con `[data-modal-open]` y cierra con `.modal__close`/overlay/Escape togglando la clase (`overlay.remove()` → `classList.add`, porque el nodo es permanente). Mata: un request HTTP por click, el truco del fragment fantasma, y el branch del controller. (b) **Names explícitos en `forms::phone`**: la derivación `base = prefix − 'Phone'` en `th:with` era ilegible y produjo 2 bugs reales al migrar (`phone`→`countryCode` no se derivaba). Nueva firma `phone(prefix, ccName, natName, dddName, ...)` — cada call site declara el contrato del wire; `th:attr="name=${x}"` omite el attr cuando es null. Ids siempre `{prefix}*` (el DDD era `{dddName}` — ahora `{prefix}Ddd`, consistente). (c) **Track sin hidden ni stripping**: inputs del componente sin `name` en markup; JS promueve el nacional a `name='telefono'` al init y reescribe su `value` a E.164 al submit. Sin JS `telefono` no viaja → `searched=false`, degradación graceful preservada (mejor que mandar el nacional crudo, que haría 500 en `PhoneNumber.normalize`). (d) **`is-hidden` honesto en SSR del DDD**: `th:classappend` según `selectedCode` en vez de oculto-siempre + reveal por JS. (e) **PIN toggle por DOM**: `.password-field__toggle` + `closest('.password-field').querySelector('input')` elimina el contrato `data-toggle-target` por string-id. (f) **`i18n.js` queda como está**: inlinear `window.uiCopies` vía `th:inline` fue evaluado y descartado — `script-src 'self'` del CSP lo bloquea; el endpoint `I18nScriptController` ES la forma CSP-compatible. Verificado Playwright 22/22: modal abre/cierra (X, overlay, Escape) sin HTMX, track UY+BR compone E.164, prefill index→/solicitar, PIN toggle |
| 139 | Eliminar HTMX por completo: `fetch` vanilla para org-options | Implementado | HTMX solo sostenía una cosa tras #138 (modal inline): el `hx-get="/solicitudes/org-options"` del select de ciudad en request-form. Reemplazado por `fetch` + `innerHTML` + re-aplicar `data-i18n` con `window.uiCopies` en `request-form.js` (~15 LOC) — el endpoint `GET /solicitudes/org-options` nunca necesitó HTMX, solo devuelve `<option>`s como fragment. Eliminados: webjar `htmx.org` del pom, `<script>` en base.html, `htmx-states.css`, listeners `htmx:afterSwap`/`responseError`/`sendError` + toast global de app.js (el toast se movió a request-form.js, su único consumidor), `hx-indicator` → `is-hidden` togglado por JS. `Routes.HTMX_ORG_OPTIONS` → `ORG_OPTIONS`. La app queda con **cero HTMX** — links, forms y fetch comunes. Hallazgo colateral al verificar: el `<option>` del fragment `ui :: options` usa `data-i18n="req_form_org_select"` cuyo valor en catálogo es "Primero seleccioná una ciudad" — la traducción post-swap sobrescribe el texto default del fragment ("Seleccioná una organización"); comportamiento idéntico al de HTMX, así que no es regresión, pero la clave i18n y el default del fragment quedaron desalineados desde antes. Verificado Playwright 23/23 (incluye `waitForResponse` del org-options 200) |
| 134 | Renombrar rutas al vocabulario de la defensa: `/solicitar`, `/mis-solicitudes`, `/rastrear`, `/acopio/solicitudes/{id}/{aceptar,rechazar,completar}`, `/mi-organizacion`, `/organizaciones`, `/salir`, `/registrarse` | Implementado | El modelo anterior mezclaba verbos y sustantivos sin criterio (`/solicitudes/nueva`, `/solicitud/{id}`, `/acopio/requests/{id}/transition?action=`, `/api/organizations/by-city`, `/logout`, `/registrar`). Ahora: las acciones principales son verbos de una palabra (`/solicitar`, `/rastrear` — las únicas URLs que un ciudadano tipearía a mano), CRUD puro queda sustantivo plural (`PUT`/`DELETE /solicitudes/{id}` vía `_method` + `HiddenHttpMethodFilter`), `/acopio/**` es el prefijo de zona org (matcher de seguridad de una línea) y las transiciones son 3 endpoints separados en vez de `?action=`. Params de query en español (`telefono`, `codigo`, `estado`, `ciudad`, `material`, `nombre`). Bonus: `/solicitud/{id}` nunca existió como GET — el redirect post-edición era un 404 latente, ahora redirige a `/mis-solicitudes`. ORG→rutas de usuario ahora responde 403 (negado por `AuthorizationFilter`, antes pasaba por `GlobalExceptionHandler`→redirect). `POST /rastrear` eliminado (el form es GET). 14 acciones / 10 recursos — ver `docs/ENDPOINTS.md` |
| 177 | Página educativa pública "Sobre los catadores" (`/sobre-catadores`) | Implementado | Página informativa que cierra la brecha entre la legislación (Oficio 044/2023, que enfatiza a catadores) y el MVP. Template `public/about-catadores.html` con estructura card-based: intro, dos columnas (rol en cadena / importancia), conexión con EcoSolicitud, y link a home. 8 claves i18n (4 secciones × 2 idiomas) agregadas a `es.json`/`pt.json`. Ruta `@GetMapping("/sobre-catadores")` en `AuthController` sin autenticación requerida, permitida en `SecurityConfig` + centralizada en `Routes.ABOUT_CATADORES`. Link agregado al footer (visible en todas las páginas). Accesible sin login, renderizado completo server-side con i18n client-side |
| 133 | Simplificar switch de idioma: links comunes en vez de máquina de estado JS | Implementado | El cambio de idioma hacía un viaje innecesario: click → JS → localStorage → `?lang=` → reload → `serverLang` → re-sync. Ahora los botones ES/PT son `<a href="?lang=es|pt">` comunes — el `LocaleChangeInterceptor` + sesión hacen todo, funciona **sin JavaScript**. `is-active` se resuelve server-side con `th:classappend`. Eliminados de `app.js`: `serverLang`, `localStorage` de idioma, click handler y `markActiveLang` (-25 líneas). El cliente ya no tiene estado de idioma: solo aplica `window.uiCopies` que el server ya sirvió en el idioma correcto |
| 132 | Fix cambio de idioma: `i18n.js` ignoraba la elección `?lang=` | Implementado | `I18nScriptController` usaba `request.getLocale()` — API servlet que lee `Accept-Language` y **ignora el LocaleResolver de Spring**: el catálogo siempre reflejaba el idioma del navegador, no el elegido. Ahora usa `LocaleContextHolder.getLocale()` (resultado de `CityAwareLocaleResolver` = sesión/ciudad). Segundo bug latente: `Cache-Control: max-age=1h` serviría el catálogo ES cacheado al cambiar a PT — resuelto haciendo la URL específica por idioma (`/js/i18n.js?l=es|pt` desde `base.html` vía `#locale.language`). Verificado en vivo: `?lang=pt` → catálogo pt, `?lang=es` → catálogo es |
| 131 | Reorganizar docs: un archivo por tema + gitflow documentado | Implementado | Los 4 docs grandes eran varios documentos pegados (patrón "doc + anexos"). Extraídos a archivos propios: `REQUISITOS.md` (catálogo RF/RN nuevo — cubría un hueco: el canonical doc `RF-RN.md` se referenciaba pero no existía), `ARQUITECTURA.md` (núcleo del sistema), `COPIES.md` (copys i18n), `TRADEOFFS.md` (26 decisiones), `CORRECCIONES.md` (hardening + limpieza), `LIMITACIONES.md` (fusionado en `DEFENSA.md` §7 desde #173, ver ese registro), `AUDITORIA.md` (inventario de superficies). MEJORAS 966→267, DEFENSA 922→271, METODOLOGIA 613→180, DIAGRAMAS 374→215 líneas. Nuevo `GITFLOW.md` + `docs/diagrams/figura5-gitflow.drawio` (registrada en `DocsController.DIAGRAMS`, visible en `/docs/diagramas` y linkeada en el footer). Cross-refs actualizados: `DEFENSA.md §N` de tradeoffs → `TRADEOFFS.md §N` (14 refs), `RF-RN.md` → `REQUISITOS.md`, convenciones en `CLAUDE.md`, 2 [FALTA] de `CONTEXTO_LLM.md` resueltos |

---

## Resumen por estado

| Estado | Cantidad |
|---|---|
| Implementado | 135 |
| Descartado | 17 |
| Diferido | 21 |
| Latente | 1 |
| Retirado | 1 |
| **Total** | **175** |

---

## Crecimientos innecesarios detectados

| Superficie | Antes | Después | Acción |
|---|---|---|---|
| Claves messages_es | 136 | 91 | Limpiado (45 muertas) |
| Claves messages_pt | 109 | 91 | Sincronizado con ES |
| Claves messages.properties | 108 | 108 | Sin cambio (fallback) |
| Claves data-i18n en templates | 227 | 227 | Sin cambio (en uso) |
| CSS clases | 292 | ~300 | +8 (dark mode + theme btn) |
| CSS líneas | 582 | ~670 | +88 (dark theme) |
| SW cache version | v14 | v17 | Bumpeado |
| Documentos docs/ | 9 | 11 | +COPIES.md, +SUPERFICIES.md |
| Diagramas activos docs/diagrams/ | 3 figuras | 4 figuras | +figura4-secuencia.drawio |
| Controllers | 16 | 17 | +DocsController |
| Templates | 27 | 29 | +docs.html, +diagrams.html |
| Endpoints públicos | 8 | 10 | +/documentos, +/diagramas |
| CI/CD | — | 1 workflow | +.github/workflows/ci.yml |

---

---

## Mejoras incrementales implementadas

### Selector de código de país en teléfono

- Se agregó un selector de país (`CountryCode`) con dos opciones: Uruguay
  (+598, 8 dígitos nacionales, primer dígito 9) y Brasil (+55, DDD de 2
  dígitos + 9 dígitos nacionales, primer dígito 9).
- `PhoneNumber.of(CountryCode, national, ddd)` normaliza el número al
  formato E.164, quitando el 0 inicial doméstico uruguayo y anteponiendo
  el DDD para Brasil.
- Los formularios de solicitud de invitado, perfil de usuario y perfil de
  organización usan el selector. La validación HTML5 (pattern) es solo
  visual; la validación real es del servidor.
- `PhoneNumber.of(String raw)` se mantiene para compatibilidad con datos
  existentes y tests previos.

### Tablero Kanban de solicitudes (CU-06)

- El tablero Kanban se integró al dashboard de organización (`/acopio/inicio`),
  no es una página aparte. La organización ve sus solicitudes agrupadas en 4
  columnas: Pendientes, En curso, Completadas, Rechazadas, directamente en
  el panel principal.
- Cada card muestra ciudad, materiales, solicitante y fecha. Los botones
  de acción llaman a los mismos endpoints de `OrgRequestController`
  (`/acopio/requests/{id}/transition`) — no duplica lógica de negocio.
- No implementa drag-and-drop; usa botones como acción intencional.
- La ruta `/acopio/kanban` fue eliminada; el template `requests-kanban.html`
  fue removido.

### 11. Política de contraseñas — mínimo 8 caracteres

- **Decisión:** la validación mínima de contraseña exige 8 caracteres.
- **Justificación:** 8 caracteres es defendible para un MVP. El demo
  password `12345678` cumple el mínimo.
- **Riesgo:** 8 caracteres sin exigir complejidad (mayúsculas, números,
  símbolos) sigue siendo vulnerable a ataques dirigidos. Esta política
  **no es aceptable para producción sin reforzarse**.
- **Para producción:** aplicar políticas OWASP: mínimo 12, complejidad,
  breach-list check. El cambio es una sola línea en
  `UserService.validatePassword()`.
- **Corrección:** el mínimo original era 3 caracteres. Se subió a 8
  en la fase de corrección (ver `docs/MEJORAS.md` §1.4).
- **Documentado como decisión consciente de fase**, no como omisión.

### 12. Blog estático — descartado del MVP

- El blog estático (`/blog`, `/blog/{slug}`) fue planificado pero
  **no se implementó**: no existe `BlogController`, ni templates, ni
  rutas. Las clases CSS de blog se eliminaron en la consolidación.
- **Razón:** el blog no aporta al flujo core (solicitud → aceptación →
  completado) y suma mantenimiento sin valor para el MVP.
- **Para producción:** si se reactiva, migrar a contenido dinámico
  (colección `posts` en MongoDB, editor en panel, slug único, fecha
  de publicación, borrador/publicado).

### 13. Catadores — CRUD descartado

- El CRUD de `InformalCollector` (`/acopio/catadores/**`) fue
  planificado pero **no se implementó**: no existe `InformalCollectorController`
  ni servicio. Quedó documentado como decisión de diseño.
- **Razón:** exponer una tabla de recolectores informales en el panel
  mezcla responsabilidades (gestión interna vs. comunicación pública) y
  no aporta al flujo principal del MVP.
- **Para producción:** decidir si el CRUD se implementa como herramienta
  interna (asignación de recolector a solicitud, RF-8 completo) o si
  se elimina definitivamente.
| 140 | Font Awesome WebJar → sprite SVG local (`/images/icons.svg`) | Implementado | FA costaba `all.min.css` (~100KB) + webfonts (~1MB) + webjar por 39 íconos de ~2.000. Los símbolos se extrajeron del propio jar (`sprites/solid.svg`) → `static/images/icons.svg` (18.8KB, 40 `<symbol>`) — íconos **idénticos**, cero cambio visual. Uso: `<svg class="icon"><use href="/images/icons.svg#{id}"/></svg>`; `.icon{width:1em;height:1em;fill:currentColor}` + `.icon--spin` reemplaza `fa-spin` (CSS propio). Dinámicos: los fragments `ui::state/row/tile` y `forms::submit` reciben el arg `icon` como id bare (`'check'`, no `'fa-check'`) y arman el href con `th:attr`; `ui::msg` mapea type→id explícito; slots de request-form mapean MANANA/TARDE/NOCHE→sun/cloud-sun/moon. JS: el PIN toggle cambia `use[href]` en vez de clases; el toast de request-form inyecta el mismo markup svg. El sprite vive en `/images/**` (ya permitAll en SecurityConfig — `/img/` habría dado 302 a login). Eliminados: webjar `fortawesome__fontawesome-free` del pom + `<link>` en base.html. Verificado: los 39 ids referenciados existen en el sprite, Playwright 27/27 (iconos con bounding box real, eye→eye-slash swap, cero requests a webjars/fontawesome), 404 sirve `compass-drafting` |
| 141 | Semántica e i18n de materiales: `fieldset/legend` + claves `mat_*` | Implementado | El grupo "Materiales" era `<div class="field">` + `<label>` sin control (inválido — los lectores de pantalla no anunciaban el grupo) → ahora `<fieldset class="field">` + `<legend>` en request-form y org/profile (reset `fieldset.field` + `.field legend` hereda estilo de label). Las claves `req_mat_*`/`mat_*` no existían en es/pt.json — las cards mostraban el enum crudo (`PLASTICO`). Agregadas `mat_*` ×7 en ambos idiomas y unificado el prefijo a `mat_` (request-form usaba `req_mat_`) — mismo material, misma palabra, misma clave |
| 142 | UX de estados vacíos y mobile: dropdown navbar, empty orgs, PIN OTP | Implementado | (a) **Dropdown mobile aplastado**: `.dropdown` usaba `left:0;right:0` dentro del `<details>` (34px de ancho = solo el toggle) → el menú salía de 34px. Ahora `right:0;width:min(20rem,100vw-2rem)` + `flex-shrink:0` en el svg del toggle. Selector huérfano `.password-field>i` → `>.icon` (quedó de la migración #140 — el candado rompía el layout del PIN). (b) **Ciudad sin organizaciones**: el select quedaba con el placeholder y `required` trancaba el submit sin explicación — `ui::options` ahora renderiza `<option disabled selected>` con `req_form_org_none` ("No hay organizaciones en esta ciudad", la clave ya existía sin uso). (c) **PIN estilo OTP**: 4 casillas `.pin-boxes__digit` + hidden `.pin-boxes__value` que JS compone (auto-avance, backspace vuelve, paste distribuye); toggle de visibilidad flipea las 4; hint `auth_pin_preview` aclara que es método provisorio de preview |
| 143 | PWA mínimo instalable (manifest + SW pass-through + botón instalar) | Implementado | Retoma #11 en versión mínima: lo que rompía antes era el *cacheo* del SW, no la instalación. `manifest.webmanifest` (nombre, theme #2d6a4f, display standalone, íconos PNG 192/512 generados del favicon.svg) + `<link rel="manifest">` + `theme-color` en base.html + permitAll en SecurityConfig. `sw.js` nuevo: conserva la limpieza del kill-switch (#100 borra caches heredados en activate) pero ya no se autodesinstala — registra un fetch handler pass-through (`respondWith(fetch())`) que es el mínimo que Chrome exige para instalabilidad. **No cachea nada**: offline la app falla igual que sin SW. Botón `[data-install-app]` oculto por defecto — JS lo muestra solo cuando `beforeinstallprompt` dispara (Chrome decide cuándo es instalable) → `prompt()` al click → oculta tras `userChoice`/`appinstalled`. Ubicación: ícono `download` en navbar actions (desktop) + `dropdown__link` en menú mobile. Clave `nav_install` es/pt. Verificado: SW registra sin errores, manifest 200, botones hidden sin prompt |
| 160 | Sacar try/catch redundante de `RequestController`/`OrgRequestController` | Implementado | `GlobalExceptionHandler` (`@ControllerAdvice`) ya tenía `@ExceptionHandler(Exception.class)` genérico (log + flash + redirect por rol vía `Routes.resolveHomeForRole`) — 5 métodos entre ambos controllers repetían ese mismo `catch (Exception e) {...}` a mano, con el mismo destino de redirect (USER→`REQUESTS`, ORGANIZATION→`ORG_REQUESTS`, verificado igual al de `resolveHomeForRole`). Se borraron esos 5 catches genéricos — el `@ExceptionHandler` global ya los cubre. El `catch (SecurityException e)` también se repetía 5 veces con mensaje/redirect fijo por controller (`RequestController`→siempre `flash.request.not_owned`+`REQUESTS`; `OrgRequestController`→siempre `flash.org.request_not_owned`+`ORG_REQUESTS`) → se extrajo a un `@ExceptionHandler(SecurityException.class)` *por controller* (no global, Spring lo resuelve igual). El `catch (IllegalStateException e)` quedó intacto: los 3 casos tienen mensaje/atributo distintos entre sí (uno usa `warningMessage` en vez de `errorMessage`), no es boilerplate copiado. Neto: ~35 líneas de try/catch menos, sin excepciones custom nuevas. Motivado por feedback externo (profesor) sobre uso de try/catch — validado contra el código real antes de tocar nada. Tests: 153/153 verdes (los 6 `*BrowserTest` que fallan necesitan Mongo local en Docker, no relacionado) |
| 144 | Fix loop infinito de redirects en login fallido + ternary prohibida dentro de `th:attr` | Implementado | Login con credenciales malas → `ERR_TOO_MANY_REDIRECTS`. Cadena causal completa: POST /entrar falla → 302 `/entrar?error` → el controller setea `errorMessage` → `base.html` renderiza `ui::msg` → **el nested ternary dentro de `th:attr` lanza TemplateProcessingException** (sintaxis `th:attr="x=@{...} + '#' + (a ? b : c)"` — el ternary parentizado rompe el parser; `${var}` simple sí funciona) → `GlobalExceptionHandler.handleGeneric` → `redirect:/entrar` (resolveTarget para anónimo) → la página renderiza con flash `errorMessage` → **misma excepción** → redirect a sí misma → loop (el flash se consume y se re-setea cada hop, nunca se agota). Fixes: (a) ternaries movidos a `th:with` en `ui::msg` y slot-icons de request-form — el `th:attr` queda `+ '#' + ${var}` idéntico al patrón probado de `ui::state`; (b) guard anti-loop `redirectOrError` en GlobalExceptionHandler — si el target == request URI renderiza `error/404` en vez de redirigir; (c) claves i18n stale `login.error`/`login.blocked`/`login.success` → `auth.login_*` (los JSON reales son `_server_auth_login_*` — el error mostraba el literal "login.error"). Verificado: POST malo → /entrar?error → 200 sin redirects, muestra "Nombre o PIN incorrectos" con `circle-exclamation` |


---

## Documentos relacionados

| 145 | `ServerMessage`: todas las claves i18n server-side nacen tipadas (mata el drift por construcción) | Implementado | Las ~60 claves i18n que emite el servidor (throws de dominio + flashes de controllers) eran strings crudos — un typo producía el literal mostrado al usuario (bugs reales: `login.error`, `mat_*`). Ahora: (a) `enums/ServerMessage` — las 67 claves como constantes con `code()` y `serverKey()`; (b) `exception/ValidationException`/`StateException`/`OwnershipException` (extienden IllegalArgument/IllegalState/Security — el dispatch existente no cambia) implementando `Keyed`; los 54 throw sites pasan a `new ValidationException(ServerMessage.X)`; (c) `BaseController`/`GlobalExceptionHandler` ganan `msg(ServerMessage)` y `msg(Throwable)` que extraen la clave via `Keyed`; (d) `UserRegistrationService.validateUserRegistration` devuelve `ServerMessage` en vez de String; (e) `Routes.resolveErrorNavigation(auth, uri)` — la decisión redirect-vs-error del handler global ahora es función pura. **El check ya atrapó bugs latentes**: 5 throws en `orElseThrow` que el patrón inicial no cubría + 3 claves sin traducción (`identity_exists`, `username_too_long`, y `password_too_long` → reusada `password_max_length` existente) + 2 `flash.*_not_found` sin clave JSON — agregadas es/pt. `ServerMessageContractTest` (unitario, sin Spring) verifica que cada `serverKey()` exista en es.json y pt.json — drift futuro = build rojo. Bonus: `th:attr` con ternary de `forms::field` movido a `th:with` (convención #144). Tests: 156/156 no-browser verdes |
| 146 | `TestFixtures` para tests unitarios: consolidar `new User()` repetido | Implementado | Auditoría de LOC de tests (motivada por comparar contra `scratch/App.java`, el sandbox de un solo archivo que replica el núcleo de dominio) encontró 251 líneas de `new User()` + setters sueltos repetidas casi idénticas en 7 archivos de test (`CityOrgServiceTest`, `RequestServiceValidationTest`, `RequestMetricsServiceTest`, `MvpRegressionTest`, `OrganizationControllerTest`, `EndToEndFlowsTest`) — el único bucket de "ceremonia de test" que era grasa evitable y no costo estructural del framework (mocks/MockMvc/Selenium sí lo son). Nueva clase `src/test/java/com/residuosolido/app/TestFixtures.java` con `citizen(id, phone)` y `organization(id, city, materials...)` — mismas firmas que `citizen()`/`org()` en `scratch/App.java`, a propósito: la idea es seguir espejando mejoras entre el sandbox y los tests reales a medida que uno valida algo en el otro. `UserServiceTest` (20 ocurrencias) quedó sin tocar — sus casos son combinaciones de campos distintas por test (username/password/email variando), no la misma forma repetida; forzarlas al fixture hubiera sido la abstracción prematura que pide evitar el CLAUDE.md. `MongoAggregationUtilsIntegrationTest` y `RequestMetricsServiceTest` (parcial) también quedaron igual por el mismo motivo. Tests: 156/156 no-browser verdes tras el cambio |

| 147 | `App.java`: consola que corre las clases REALES del dominio (no espejo) | **Retirado** (ver #153) | Primera versión era un espejo (~300 LOC duplicando reglas) — mismo defecto de drift que #145 eliminó, borrada. Versión final: `javac -cp "target/classes:$(cat cp.txt)" App.java` — importa `PhoneNumber`, `Request.accept/reject/complete`, `Request.assignOrganization`, `ServerMessage`, excepciones `Keyed`, y `RequestService` instanciado con repos `null` (sus métodos de validación son puros). Lo único propio: persistencia `Map`/`List` (reemplaza Mongo) y menú `Scanner` (reemplaza HTTP). Si una regla del dominio agarra dependencia de Spring, App.java deja de compilar — es el test de pureza del núcleo. Terminó compitiendo con `scratch/App.java` por el mismo nombre en la raíz del repo, dos filosofías distintas para el mismo propósito — se decidió quedarse con una sola (#153). `cp.txt` sigue vivo porque también lo usa `scratch/RenderFragment.java` (#148) |

| 148 | `scratch/RenderFragment`: render de un fragment Thymeleaf sin levantar Spring | Implementado | El "App.java del tejido": el núcleo se itera removiendo el framework, pero el tejido ES el framework — se aísla la sub-capa mínima que expresa el bug. `TemplateEngine` pelado + `FileTemplateResolver` a los templates reales + `MockServletContext`/`MockHttpServletRequest` (spring-test) → renderiza `fragments/X :: frag` con vars arbitrarias en ~50ms vs minutos de boot+navegación. Uso: `java -cp "target/classes:scratch:$(cat cp.txt)" RenderFragment "fragments/ui :: msg"`. El bug del loop de login (ternary anidado en `th:attr` de `ui::msg`, un fragment que solo renderiza cuando hay error) habría explotado acá en la primera ejecución — es la herramienta para ejercitar fragments de paths de error (msg, options vacío, state) sin boot ni browser. Verificado: `ui::msg` resuelve `circle-exclamation` vía `th:with`, `ui::options` muestra el `<option disabled>` del empty state, `ui::state` renderiza ícono+texto |
| 149 | `TemplateI18nContractTest`: mismo contrato que #145 pero para `data-i18n` client-side | Implementado | Las 227 claves `data-i18n`/`data-i18n-attr` de los templates (125 únicas) no tenían ningún check — el mismo tipo de bug que `ServerMessage` (#145) resolvió del lado servidor (`mat_*`, `login.error`) podía colarse acá sin que ningún test lo note. Nuevo `src/test/java/com/residuosolido/app/i18n/TemplateI18nContractTest.java`: escanea todos los `.html` de `templates/` con regex, extrae cada clave literal y verifica que exista en `es.json`/`pt.json` — test unitario puro, sin Spring, ~2s. Alcance: solo claves literales; las interpoladas vía `th:attr` con expresión (`'mat_' + mat.name()` en forms/ui/profile/request-form/track) no se cubren, dependen de un enum, no de un string fijo — candidato para un contrato aparte si hace falta. Corrida inicial: 125/125 claves ya traducidas en ambos idiomas, sin drift nuevo (a diferencia de #145) — el valor es dejar el contrato armado para que el próximo typo sí lo frene. Tests: 158/158 no-browser verdes |
| 150 | Refactor por delta contra `scratch/App.java`: 2 hallazgos en `RequestService`/`RequestRepository` | Implementado | Estrategia "portá una clase Core al sandbox y la diferencia es tu lista de qué sacar" aplicada a `RequestService` (309 líneas reales vs ~215 del equivalente en `scratch/App.java` para las mismas reglas). Dos hallazgos reales, no solo LOC: (a) `RequestRepository.findByGuestPhoneOrderByCreatedAtDesc` — método derivado de Spring Data sin ningún caller en `src/main` ni `src/test`, y que además **contradice RN-3** ("el teléfono solo no es suficiente para rastrear, hace falta código privado también") — quedaba como una vía de acceso que ningún código usa hoy pero que alguien podría llamar por error y reintroducir el hueco de seguridad que RN-3 explícitamente cierra. Borrado. (b) `RequestService.getRequestsByOrganizationAndStatus` — método público sin caller externo (solo lo llamaba su propio sibling `getOrgRequestsByStatusFilter`), inlineado. De paso, `validateCreate`/`validateUpdate`/`validateMaterials`/`validateEstimates` bajaron de `public` a package-private: sin caller externo salvo un test del mismo paquete (`MvpRegressionTest`), que sigue funcionando igual. Verificado contra código real con grep antes de tocar nada (mismo criterio que #144). Tests: 158/158 no-browser verdes |

| 151 | `scratch/FrontendCheck`: validación de contratos frontend sin browser ni Spring | Implementado | Las clases de bug "contrato implícito entre capas" (selector CSS huérfano `.password-field>i`, sprite en `/img/`→302, id JS sin elemento, fragment renombrado) son detectables por puro parseo de archivos — no hace falta renderizar. Scratch de ~140 LOC que cruza: (a) `getElementById`/`querySelector('#x')` en JS ↔ `id=`/`th:id`/ids-derivados-de-prefix (`forms::phone('x'`→`xNational`...) en templates; (b) `icons.svg#id` referenciado ↔ `<symbol id>` definido; (c) `var(--x)` usada ↔ `--x:` definida; (d) `th:fragment="x"` definido ↔ `:: x(` llamado; (e) selectores CSS por tag obsoleto (`>i` era Font Awesome). Corrido con bugs sintéticos inyectados: detectó los 3 (icono inexistente, var CSS sin definir, id JS huérfano) — checker verificado no-vacuo. Alcance: contratos, no layout — el dropdown de 34px solo lo ve un browser real (Playwright). Correr: `javac -d scratch scratch/FrontendCheck.java && java -cp scratch FrontendCheck` — no necesita classpath de dependencias |
| 152 | `OrphanI18nKeysTest`: contrato inverso a #145/#149 — 51 claves i18n muertas encontradas y borradas | Implementado | Estrategia "contratos inversos": en vez de "¿toda clave declarada existe traducida?" (#145/#149), pregunta "¿toda clave traducida se usa en algún lado?" — mismo tipo de limpieza que se hizo a mano una vez (#24: 136→91 claves), ahora automática. Nuevo `src/test/java/com/residuosolido/app/i18n/OrphanI18nKeysTest.java`: "usada" = serverKey() de un `ServerMessage`, `data-i18n`/`data-i18n-attr` literal en un template, coincidencia con un prefijo dinámico (`'mat_'`/`'city_'`/`'req_city_'`/`'req_form_slot_'` + cualquier enum de dominio, expandido contra `.values()` real — no regex sobre el `.java`), o literal citado en cualquier lado de un template (cubre claves pasadas como argumento a fragments, ej. `forms::submit(icon, 'auth_login_button', ...)`). Corrida inicial encontró 54 huérfanas: 21 mensajes de error server-side legacy (`_server_flash_org_request_load_error` y similares — reemplazados por el `flash.error.generic` genérico que dejó #150/#144, nunca se borró la traducción vieja) + 30 copies muertas de auth/perfil/dashboard (`auth_join_title`, `profile_title`, `dash_title`...) + `track_no_results_desc`. **De esas 54, 3 (`req_success_title`, `track_no_results`, `profile_title`) están referenciadas por locators de browser tests (`CitizenBrowserTest`, `GuestBrowserTest`) que esperan un `data-i18n` que HOY no existe en ningún template real** — no se borraron (el contrato ahora también escanea `src/test/java` para no confundir "huérfana" con "test desincronizado del template"), pero es un hallazgo aparte que merece revisión: o el browser test quedó desactualizado tras un rename, o al template le falta ese elemento. Como los `*BrowserTest` necesitan Mongo en Docker (no disponible en este entorno), no se pudo correr para confirmar cuál de las dos. Las otras 51 sí se borraron de `es.json`/`pt.json` (280→229 claves c/u). Tests: 160/160 no-browser verdes |
| 153 | "Solo puede haber uno": consolidación de los 2 `App.java` en `scratch/App.java` | Implementado | Habían quedado dos archivos `App.java` compitiendo por el mismo rol — el de la raíz (#147, importaba clases reales, cero drift posible pero necesita compilar Spring) y `scratch/App.java` (reimplementación standalone, sin build, con harness de 64 escenarios + modo consola). Ya establecido en esta sesión que `scratch/App.java` es la fuente de verdad del núcleo — se retiró el de la raíz. `cp.txt` no se tocó: lo sigue usando `scratch/RenderFragment.java` (#148). Trade-off aceptado: se pierde la garantía de "cero drift por construcción" del de la raíz — el precio ya se pagó una vez (el límite de nombre de invitado quedó en 80 en el sandbox vs 100 en `RequestService.java` real, sin portar) y se corrige a mano cuando se detecta, no automáticamente |
| 154 | Consolidación de gaps de dominio en `pseudoapp.java` → traducidos a `scratch/App.java` | Implementado | Auditoría `docs/sincronizacion-codigo-texto.md` (18 hallazgos) cruzada con `docs/gaps-detalle.md`: de los 18, solo 2 eran lógica de dominio real no modelada — LOGIN/LOCKOUT (`RateLimiter.java`: 3 intentos, 15 min, no reincrementa si ya estaba bloqueado, reset total en éxito) y REGISTER (`UserRegistrationService.java`: username+PIN 4 dígitos+teléfono, sin email, orden exacto de validación, rate limit 5/min por IP). El resto son texto de tesis o frontend/infra, quedan fuera del pseudocódigo de dominio a propósito. Cambios: (a) `weight`/`volume` degradados a `// DIFERIDO` en `REQUEST.create`, se sacó `validateEstimates` y los 2 campos de `Request` — ya no se valida ni persiste (alineado con "fuera de alcance" del artículo de tesis); (b) notación de error migrada de `-> error.xxx` sueltos a un tipo único `ServerMessage<T>` con 5 categorías provistas + **`RateLimitError` como 6ª categoría nueva** (lockout/rate-limit no encaja en Validation/NotFound/Authorization/StateTransition/Concurrency); (c) nueva `RateLimitException` en el sandbox Java para esa categoría. Hallazgo extra no listado en la auditoría original: `UserService.updateUser` tiene una validación de password de 8+ caracteres sin ningún caller real, que contradice el PIN de 4 dígitos — decisión cerrada (no diferida): es código muerto, se borra de `UserService.java` cuando se porte, no entra al pseudocódigo. Marcadores `DIFERIDO`/`DESCARTADO` agregados donde había punto de enganche real: recuperación de PIN, verificación de legitimidad de org, recolectores informales, optimización de ruta, historial de transiciones, notificaciones, métricas públicas por ciudad. Escenarios nuevos en `scratch/App.java`: registro (feliz + 5 validaciones + rate limit) y login (feliz + lockout a los 3 intentos + no reincrementa bloqueado + reset en éxito), más probado a mano en el modo consola. `scratch/pseudoapp.java` 122→255 líneas, `scratch/App.java` 939→1082 líneas. Cobertura: 78/78 escenarios, 42/42 error-keys, 29/29 branches |
| 155 | Ejecutado en Spring real: borrado de `UserService.validatePassword` (decisión de #154) | Implementado | LOGIN y REGISTER ya existían en el Spring real (de ahí se extrajo la lógica para #154 — nada que portar); lo que sí quedaba pendiente era ejecutar la decisión "se borra cuando se porte" sobre el código muerto. `UserService.updateUser(user, newPassword)` → `updateUser(user)`: sacado el parámetro, la rama `if (newPassword...)`, `validatePassword()`, el campo/import `PasswordEncoder` (sin otro uso en la clase) y las 2 constantes `ERROR_REGISTER_PASSWORD_MIN_LENGTH`/`MAX_LENGTH` de `ServerMessage.java` (sin otro throw site). Limpieza en cascada, siguiendo el mismo criterio de #150/#152: 2 claves i18n huérfanas borradas de `es.json`/`pt.json` (227→227, ya contaban desde #152), 2 filas de `I18nMessageResolutionTest` que referenciaban la clave muerta, y 4 tests de `UserServiceTest` que ejercitaban exclusivamente la rama borrada (`updateUser_shortPassword_throwsException`, `_validPassword_encodesAndSaves`, `_nullPassword_doesNotChangePassword`, `_emptyPassword_doesNotChangePassword`) — probaban comportamiento que ya no existe, no un caso de uso real. Verificado con grep que no quedó ningún caller de la firma vieja antes de tocar nada. Tests: 154/154 no-browser verdes |
| 156 | Gap nuevo (no en la auditoría original): email opcional en perfil — formato + unicidad | Implementado | Encontrado leyendo `User.java`/`UserService.java` con el mismo criterio que destapó el "password fantasma" (#155), no vino de `docs/sincronizacion-codigo-texto.md`. Regla real: `User.setEmail()` — vacío/null es no-op válido (el registro, #154, no pide email), si no vacío normaliza a minúsculas y valida `^[^\s@]+@[^\s@]+\.[^\s@]+$` + largo≤254 (si no: `ERROR_REGISTER_EMAIL_INVALID`); `UserService.updateUser` chequea unicidad case-insensitive excluyendo al propio usuario (si no: `ERROR_REGISTER_EMAIL_EXISTS`) recién ahí asigna. Agregado a `docs/gaps-detalle.md` (A4), `scratch/pseudoapp.java` (bloque USER) y `scratch/App.java` (`User.setEmail`/`normalizeAndValidateEmail`, `checkEmailUnique`, `updateProfile` con el parámetro nuevo). **Bug real encontrado armando el escenario de "unicidad": la primera traducción mutaba `user.email` en `setEmail()` antes de chequear unicidad — un email duplicado dejaba el campo mutado igual aunque la operación fallara.** Confirmado que el Spring real NO tiene este bug (separa el objeto transitorio, ya validado por el caller, del persistido, que solo se muta después de pasar la unicidad) — era un artefacto de haber colapsado ambos objetos en uno solo en el sandbox. Arreglado separando validación-sin-mutar (`normalizeAndValidateEmail`) de la asignación final. No hizo falta tocar Spring real — la lógica ya estaba bien ahí. Tests: 83/83 escenarios, 44/44 error-keys, 33/33 branches |
| 157 | Limpieza de tejido conectivo: 4 hallazgos (duplicación de `msg()`, `GlobalErrorController` sin guarda anti-loop, método muerto) | Implementado | Auditoría de la capa de infraestructura (Strategy 1 aplicada a `SecurityConfig`, `WebConfig`, `RateLimiter`, `DataLoader`, `MongoIndexMigration`, `CityAwareLocaleResolver`, controllers/servicios de soporte) buscando callers reales de cada método público. Hallazgos: (a) `GlobalErrorController.handleError` reimplementaba a mano la lógica de `Routes.resolveErrorNavigation` (auth-check + redirect por rol) en vez de reusarla — y al hacerlo perdía la guarda anti-loop que esa función tiene a propósito (misma familia de bug que el loop infinito real de #144). Ahora reusa `Routes.resolveErrorNavigation` directo. (b) `msg(ServerMessage key)` estaba triplicado (`BaseController`, `GlobalErrorController`, `GlobalExceptionHandler`) — y la copia de `GlobalErrorController` usaba la variante de `getMessage()` de 3 argumentos (sin fallback), a diferencia de las otras dos (4 argumentos, fallback al propio código) — podía tirar `NoSuchMessageException` justo en la página de error si faltaba una clave. (c) `msg(Throwable e)` duplicado en `BaseController`/`GlobalExceptionHandler`. Ambos controllers de error ahora extienden `BaseController` (antes no lo hacían pese a ser candidatos naturales) y heredan una sola implementación. (d) `MongoAggregationUtils.extractSimpleCount` — método público estático sin ningún caller en `src/main` ni `src/test`, borrado. **Descartado tras verificar:** bajar `LocalImageService.uploadFile` a `private` — tiene su propia suite de tests dedicada (`LocalImageServiceTest`, 8 casos) que lo llama como API pública; no es una fuga de visibilidad, es una unidad de comportamiento con cobertura propia. Tests: 154/154 no-browser verdes |
| 158 | Bug real reportado por el usuario: perfil de organización se guardaba sin ciudad/teléfono, sin avisar | Implementado | El cartel de onboarding en `org/profile.html` dice "Necesitamos estos datos para que los ciudadanos puedan contactarte", pero el `<select id="city">` no tenía `required` y el fragment de teléfono se llamaba con `required=false` — el form guardaba igual sin esos datos, dejando el perfil incompleto en silencio (sin decir qué faltó). No era un mismatch nuevo de código — coincidía con la decisión ya documentada en SYNC-13 (onboarding implícito vía `isAvailable()`, no forzado en el guardado), pero el usuario decidió subir el nivel de exigencia: ciudad y teléfono pasan a ser **siempre** obligatorios en este form (no solo mientras el perfil está incompleto). Cambio: `required` en el `<select>` y en el input de teléfono (`org/profile.html`), más validación server-side en `OrgProfileController.updateOrgProfile` (throws `ValidationException(ERROR_PROFILE_PHONE_REQUIRED)`/`ERROR_PROFILE_CITY_REQUIRED` si faltan — mismas claves que ya usa `User.completeProfile()`, sin i18n nuevo). La validación se agregó en el controller, no en `UserService.updateProfile` — ese método es compartido con `RequestCreateController` para actualizar el teléfono de un ciudadano (`city=null` intencional ahí), forzarlo ahí hubiera roto ese flujo. Tests: 154/154 no-browser verdes (los existentes que hacen PUT a `/mi-organizacion` ya mandaban phone+ciudad) |
| 159 | Resuelta pregunta abierta de #152: `req_success_title`/`track_no_results`/`profile_title` eran tests obsoletos, no bugs de template | Implementado (parcial) | Se pudo correr `*BrowserTest` con Mongo real (Atlas vía `.env`) y encontrar 4 causas raíz que bloqueaban TODA la suite antes de siquiera llegar a esas 3 claves: (a) `PlaywrightBaseTest` tenía la URI de Mongo hardcodeada a una IP de Docker (`172.17.0.2`) en vez de `${SPRING_DATA_MONGODB_URI}` como el resto de los tests — ahora alineado; (b) `login()`/2 tests sueltos llenaban `input[name='password']`, que ya no existe — el login es 4 cajitas de PIN (`fragments/forms :: pin`) con un hidden sincronizado por JS — nuevo helper `fillPin()` compartido; (c) **`DataLoader` sembraba usuarios con password de 8 caracteres (`"12345678"`) — bug real, no solo de test: ningún usuario seed podía loguearse por la UI real desde que el sistema migró a PIN de 4 dígitos.** Cambiado a `"1234"` en `DataLoader.java` (real) + 4 tests + `docs/DEFENSA.md`/`docs/CORRECCIONES.md` (con nota de que la sección 1.4 de CORRECCIONES describe una validación ya borrada en #155, no se reescribió la historia). (d) `#city` → el id real es `#ciudad` (`fillRequestForm`/`fillGuestRequestForm`). Con las 4 causas resueltas, `CitizenBrowserTest.citizenEditsRequest` **llegó hasta la aserción real** y confirmó: tras crear una solicitud, `RequestCreateController` redirige a `/mis-solicitudes` o `/rastrear` con un flash message (`FLASH_REQUEST_CREATED`) — nunca existió una pantalla de éxito dedicada con `req_success_title`. El patrón de UI que el test esperaba fue reemplazado por redirect+flash; el test nunca se actualizó. Mismo diagnóstico aplica a `track_no_results`/`profile_title` (no verificados con el mismo nivel de detalle, pero mismo patrón: `profile_title` no existe en ningún template, `org/profile.html` usa `org_profile_title`). **Pendiente:** `OrganizationBrowserTest` quedó 5/5 verde; los otros 5 archivos aún tienen fallas menores no investigadas a fondo (ej. `citizenEditsRequest` probablemente choca con `#ciudad`/`#organizationId` deshabilitados en modo edición — `th:disabled="${isEdit}"`) — actualizar las 3 aserciones obsoletas y el resto de la suite queda como trabajo futuro, no se tocó en esta pasada |
| 161 | Bug real reportado por el usuario: ciudad mostraba `LIVRAMENTO` crudo (enum) en vez de "Sant'Ana do Livramento" | Implementado | El select de ciudad en `request-form.html` emitía `data-i18n="req_city_livramento"` vía `th:attr` dinámico, pero **ninguna clave `req_city_*` ni `city_*` existía en los JSON** — i18n.js no encontraba traducción y quedaba el `th:text="${c.name()}"` crudo del enum. Mismo hueco en `req_form_slot_*` (franjas mostraban `MANANA`/`TARDE`/`NOCHE`). Era el hueco conocido del contrato: `TemplateI18nContractTest` solo verificaba claves literales, no las interpoladas `'prefix_' + enum.name()`. Fix doble: (a) 8 claves agregadas en `es.json`/`pt.json` (`req_city_rivera/livramento`, `city_rivera/livramento`, `req_form_slot_manana/tarde/noche`); (b) `TemplateI18nContractTest` extendido — extrae prefijos dinámicos de los templates con regex `'([a-z_]+)'\s*\+`, los expande contra un registro explícito `DYNAMIC_PREFIX_ENUMS` (mat_→MaterialCategory, city_/req_city_→City, req_form_slot_→TimeSlot) y exige cada clave expandida en ambos JSON; un prefijo nuevo sin registrar rompe el build. Verificado en browser: select muestra "Rivera"/"Sant'Ana do Livramento". Tests: 7/7 contract verdes |
| 162 | `DocsContractTest`: el drift docs↔código se convierte en build rojo + saneamiento de toda la capa `.md` | Implementado | Mismo patrón que los contract tests de i18n (#145/#149/#152): la convención "actualizar el doc al cambiar código" falla en silencio — la auditoría SYNC encontró el doc de tesis congelado, pero la misma enfermedad estaba en los docs canónicos (`RequestQueryService`/`RequestTransitionService`/`PublicMetricsService`/`GuestRateLimiter`/`LoginAttemptService`/`OrgDashboardController` descritos como vigentes en ARQUITECTURA/METODOLOGIA/REQUISITOS/TRADEOFFS/DIAGRAMAS/AUDITORIA/CONTEXTO_LLM, `InformalCollector` "latente" pese a estar borrado, HTMX + `htmx-states.css` inexistente en CLAUDE.md, IDs duplicados #144/#150/#159 en esta tabla, 3 docs fuera del INDICE). Nuevo `src/test/java/com/residuosolido/app/docs/DocsContractTest.java` (4 checks, ~200 LOC, test unitario puro sin Spring): (a) IDs de filas de esta tabla únicos — ya atrapó el duplicado #159 de esta misma pasada; (b) todo `docs/**/*.md` + CLAUDE/README listado en INDICE; (c) nombre de clase backtickeado → existe como tipo declarado en src/scratch, es framework/constante-enum/término-de-dominio (whitelists), o el contexto lo marca histórico ("eliminado", "fusionado en", "no existe") — mencionar una clase muerta solo vale si decís que está muerta; (d) path backtickeado → existe en disco, resolviendo el shorthand `static/`/`templates/` bajo `src/main/resources/`, sufijos `:línea` y marcadores "eliminado" (archivos citados como borrados son legítimos). Saneamiento aplicado en la misma pasada: ~15 referencias stale corregidas (services consolidados → `RequestService`/`RateLimiter`/`AuthenticationEventHandler`, paths `i18n/common/`→`i18n/`, `theme.js`/`figuras.html` marcados o corregidos, CLAUDE.md sin HTMX, COPIES.md degradado a índice editorial, CONTEXTO_LLM marcado como derivado). Tests: 4/4 verdes |
| 163 | `RequestServiceTest` nuevo: cierre de la brecha de cobertura de `RequestService` (41%→90% instrucciones, 40%→73% ramas) | Implementado | Auditoría de deuda técnica cruzando `docs/BOILERPLATE_VS_CORE.md` (clasifica `RequestService` como "el corazón" del sistema, 329 líneas) contra el reporte JaCoCo real: era el servicio core con menor cobertura de ramas de todo el dominio. `RequestServiceValidationTest` ya cubría las validaciones RN-10/RN-11 (materiales/dirección/organización requeridos, borrado solo en PENDING) pero dejaba sin ejercitar: ownership (`getOwnedRequest`/`getOwnedOrgRequest` — no encontrado, pertenece a otro, solicitud de invitado sin dueño), transiciones de estado a través del servicio (`acceptRequest`/`rejectRequest`/`completeRequest` con `TimeSlot`, y sus `StateException` por transición inválida), concurrencia optimista (`OptimisticLockingFailureException` → `StateException` tanto en `deleteOwnedRequest` como en las transiciones), consultas de invitado (`getGuestRequests` con teléfono/código en blanco vs. normalizado), el filtro de status de organización (`getOrgRequestsByStatusFilter` — vacío, válido, inválido con fallback), estimaciones inválidas (peso/volumen fuera de catálogo), guest con nombre demasiado largo o teléfono inválido, organización sin materiales aceptados, y el flujo completo de `createRequestWithImage`/`updateRequest` (validación de imagen antes de crear, adjuntar imagen post-creación, reasignación de organización al editar). 34 tests nuevos, ningún cambio en código de producción — la clase ya se comportaba correctamente, solo faltaba el arnés de tests. Tests: 192/192 no-browser verdes (158→192) |
| 164 | Cobertura de `UserService` (49%→98% instrucciones, 53%→93% ramas) | Implementado | Mismo criterio que #163: `UserServiceTest` ya cubría `updateProfile` (auto-completado de perfil de org con phone+city) pero dejaba sin ejercitar el resto de la clase — `findAuthenticatedUserByUsername` (found/not-found → `ValidationException`), `isAnonymous`/`resolveUser` (null, `"anonymousUser"`, principal real — la resolución del usuario autenticado nunca se había probado), y `updateUser` directo: usuario no encontrado, email ya usado por **otro** usuario (throws `ERROR_REGISTER_EMAIL_EXISTS`), mismo email del propio usuario (el `filter(other -> !other.getId().equals(existing.getId()))` no debe romper al guardar sin cambiar el email), email `null` (no debe consultar `findByEmailIgnoreCase` en absoluto), y `profileCompleted` explícito en `false` pisando un `true` existente. También se cubrió el overload de 5 argumentos de `updateProfile` seteando email, que no tenía ningún test. 13 tests nuevos, ningún cambio en código de producción. Tests: 205/205 no-browser verdes (192→205) |
| 166 | 2 de 4 micro-hallazgos de auditoría de plomería aplicados a `src/main` (bugs silenciosos en potencia); los otros 2 quedan pendientes a propósito | Implementado | Auditoría cruzada de `config/`+`controller/` buscando duplicación real (no solo tamaño) encontró 4 hallazgos; se decidió aplicar solo los 2 con riesgo real de bug silencioso, dejando los 2 de pura limpieza/estilo sin tocar para minimizar el diff de `src/main` (contexto: tesis en curso, ratios de repo bajo revisión). Aplicados: (b) la lista de rutas "solo invitado" (`/`, `/index`, `/entrar`, `/registrarse`) estaba hardcodeada en 2 lugares (`WebConfig.addInterceptors` y `AuthNavigationInterceptor.GUEST_ONLY_PATHS`) — si alguien agregaba una ruta nueva y tocaba solo uno de los dos, el interceptor quedaba desincronizado en silencio; movida a `Routes.GUEST_ONLY_PATHS`, construida a partir de las constantes `HOME`/`INDEX`/`LOGIN`/`REGISTER` ya existentes (no un tercer literal nuevo). (c) `OrgProfileController` reseteaba el locale de sesión con el string literal `"org.springframework.web.servlet.i18n.SessionLocaleResolver.LOCALE"` en vez de la constante `SessionLocaleResolver.LOCALE_SESSION_ATTRIBUTE_NAME` que `CityAwareLocaleResolver` ya usa correctamente — mismo valor hoy, pero sin fuente única de verdad. **Pendientes a propósito, no implementados:** (a) `JsonMessageSource`/`UiCopyCatalog` parseando el mismo JSON i18n dos veces con código casi idéntico (se había extraído a un `I18nCatalogLoader` compartido en un intento previo, revertido); (d) constructor `@Autowired` vacío sin efecto en `OrgProfileController`. Tests: 205/205 no-browser verdes, sin cambios en el conteo |
| 167 | Matriz de colisiones entre actores (`java scratch/App.java actores`) + fix de 2 conflictos sin traducir que el modelo expuso | Implementado | Nuevo modo del sandbox que siembra datos (2 ciudadanos, 2 orgs, requests frescas por escenario) y corre pares de acciones **intercalados** sobre la misma entidad (ambos actores leen la misma versión en T0, A commitea, B commitea sobre su copia stale — el lost-update que `@Version` detecta). 15 escenarios en 5 grupos (org×org, ciudadano×org, mismo ciudadano×2 dispositivos, identidad, interferencia de dominio). **Hallazgos verificados en el código real:** (a) `RequestService.updateRequest` hacía `save()` sin catch — edición concurrente (vs. aceptación, vs. otra edición, vs. borrado) → `OptimisticLockingFailureException` pelada → `GlobalExceptionHandler` genérico (~500) en vez del flash `concurrent_modification` que sí usan delete y transiciones; fix: `saveWithOptimisticLock` ahora devuelve `Request` y `updateRequest` lo usa. (b) `UserService.updateUser` hacía check-then-act de email + `save()` sin catch — dos perfiles con el mismo email en la misma ventana → `DuplicateKeyException` del índice único sparse → ~500 en vez de `email_exists`; fix: catch → `ValidationException(ERROR_REGISTER_EMAIL_EXISTS)` (asimetría corregida: `AuthController:57` ya atrapaba DuplicateKey en register). Interferencia semántica declarada, no corregida: `accept` no re-valida `acceptedMaterials` — una solicitud conserva los materiales con que fue creada (defendible). Sandbox actualizado para espejar el estado post-fix: la matriz ahora muestra 0 conflictos sin traducir y sirve como regresión (si un write path pierde su catch, el escenario vuelve a mostrar RAW). 2 tests nuevos (`updateRequest_concurrentModification`, `updateUser_emailTakenInRace`). Tests: 207/207 no-browser verdes (205→207) |
| 168 | Footer con enlaces a toda la documentación + sandbox (`scratch/`), para leer desde el celular | Implementado | Pedido explícito del usuario: acceso rápido a todos los docs y al pseudocódigo sin tener que abrir el repo. El footer ya tenía 5 links "TEMPORAL" (ver comentario existente, a sacar antes de usuarios reales) a un subconjunto de docs — se completó con los 17 `.md` whitelisteados en `.gitignore` (los únicos que existen también en Render) más el visor `/docs/diagramas` ya existente. Se excluyó a propósito `docs/tesis/CONTEXTO_LLM.md` (es contexto para continuidad entre sesiones de agente, no lectura humana) y `docs/gaps-detalle.md`/`docs/sincronizacion-codigo-texto.md` (gitignored, no whitelisteados — funcionarían en local pero 404 en prod). Para `scratch/App.java` y `scratch/pseudoapp.java` (gitignorados, `/scratch/` completo) se generalizó `DocsController.serveDoc()` → `serveFile(baseDir, relativePath, mediaType)` (antes solo aceptaba `DOCS_DIR`) y se agregó `Routes.SCRATCH_FILE` (`/scratch/{file}.java`) + permiso `permitAll` en `SecurityConfig` para `Routes.SCRATCH_ANY`. **Advertencia explícita en el Javadoc del método:** funciona en local/dev porque lee del disco, pero da 404 en Render porque `scratch/` nunca se sube (gitignoreado a propósito, ver el entonces `docs/GITFLOW.md` — migrado a `docs/referencia/GITFLOW.md` desde #173). Verificado contra la instancia local ya corriendo (hot reload activo, no hizo falta reiniciar): `/scratch/pseudoapp.java` y `/docs/INDICE.md` responden 200 con `Content-Type: text/plain`/`text/markdown`. Tests: 207/207 no-browser verdes, sin cambio de conteo (0 tests nuevos, es una feature de navegación) |
| 169 | Bug real reportado por el usuario: "pongo bien mi número de teléfono y no me deja solicitar" — 3 causas encontradas en la misma pasada | Implementado | Reporte del usuario en 3 mensajes sucesivos que resultaron ser 3 bugs reales distintos, todos en el flujo de `/solicitar`: **(1)** `app.js` le ponía un atributo HTML5 `pattern` al input de teléfono (`PHONE_PATTERNS`) que para `+598` solo aceptaba `[0-9 ]` — un número escrito con guiones o paréntesis (formato común: `(099) 123-456`) hacía que el navegador bloqueara el submit en silencio con su tooltip nativo, sin llegar nunca al servidor (`request-form.html` no tiene `novalidate`). Afecta los 4 formularios que usan el componente de teléfono (solicitar, registro, perfil de org, rastrear). Fix: patrón ampliado a `[0-9 \-()]` con rango de longitud más laxo. **(2)** `RequestCreateController.createRequest`, en el catch de `IllegalStateException`, redirigía siempre a `Routes.REQUESTS` (`/mis-solicitudes`, `hasRole('USER')`) sin importar si el actor era invitado — un invitado que dispara una `IllegalStateException` acá (ej. `LocalImageService.attachImageToRequest` envolviendo una falla de escritura en disco) queda con Security bloqueándole el acceso a esa URL y lo rebota a `/entrar`: "me manda a iniciar sesión" sin ningún mensaje que explique la causa real. Fix: el destino ahora depende de `userService.resolveUser(authentication)` — invitado va a `REQUESTS_NEW`, logueado a `REQUESTS`. **(3)** La causa raíz de fondo, la que el usuario señaló como "puede explotar en otros lugares" (correcto): `PhoneNumber.java` limpiaba `[\s-]` (espacios y guion) en 2 lugares (número nacional y DDD) mientras `app.js` ya limpiaba `[\s\-()]` (con paréntesis) en sus propios 2 lugares — la misma responsabilidad ("sacar separadores decorativos de un teléfono") reimplementada 4 veces sin estar sincronizada, mismo patrón de causa raíz que #166. Un número con paréntesis pasaba la limpieza del cliente pero fallaba `cleaned.matches("[0-9]+")` en el servidor, tirando `ERROR_PHONE_INVALID` con el mensaje "El teléfono debe incluir código de país" — que ni siquiera describe la causa real (ese código de error se usa para 2 fallos distintos: E.164 mal formado en el overload de 1 argumento, y caracteres no numéricos tras la limpieza en el overload país-aware). Fix: constante `DECORATIVE_CHARS` (`[\s\-()]`) centralizada en `PhoneNumber`, aplicada en los 3 puntos de limpieza del servidor — ahora servidor y cliente están de acuerdo. Severidad evaluada explícitamente para el usuario: no es un bug de seguridad/integridad (no guarda datos corruptos, no hay bypass), es un bug de UX + duplicación de lógica sin sincronizar — la misma familia de causa raíz que #166, no una nueva clase de problema. 3 tests nuevos en `PhoneNumberCountryCodeTest` (paréntesis en UY, paréntesis en nacional+DDD de BR, paréntesis en el overload de 1 argumento). Tests: 210/210 no-browser verdes (207→210) |
| 170 | Instrumento de evaluación de usabilidad: formulario Likert por funcionalidad (`docs/USABILIDAD.md`) | Implementado | Pedido del profesor: cada funcionalidad visible del sistema debe tener un formulario Likert de usabilidad para usuarios finales. Doc nuevo con 8 bloques (registro, login, solicitud invitado, rastreo, mis solicitudes, panel org, perfil org, idioma/general), escala 1–5 con ítems invertidos (R) para detectar respuestas mecánicas, criterio de lectura (media ≥4 usable, ítem ≤3 = fricción → candidato a esta tabla), datos de encuestado, preguntas abiertas y guía de aplicación (5–8 usuarios por rol, Nielsen ~85%). El ítem de teléfono del F3 documenta la robustez server-side (`DECORATIVE_CHARS`) como algo que el usuario *no debería* notar. Límite honesto declarado en el propio doc: es el instrumento — aplicarlo con usuarios reales sigue siendo trabajo futuro, consistente con LIMITACIONES/DEFENSA. Tests: 210/210 no-browser verdes, sin cambio de conteo (doc nuevo, 0 tests) |
| 171 | Dataset de demo armado por propósito para la defensa (`DataLoader.seedDefenseDemoData`) + 2 gaps de modelo documentados | Implementado | Pedido explícito del usuario: dataset "por propósito, no por volumen" para la exposición de tesis (contexto Rivera/Sant'Ana do Livramento). Antes de escribir código se leyeron los enums/modelo reales y se mapeó cada caso pedido — 2 no tienen representación posible y se dejaron documentados en vez de resueltos con código nuevo: **(a)** "REJECTED con motivo escrito" — `Request`/`RequestStatus` no tienen campo de motivo de rechazo, es un booleano puro hoy; **(b)** "usuario con locale pt" — `User` no tiene campo `locale`, el idioma se resuelve en runtime por la ciudad del usuario logueado (`CityAwareLocaleResolver`), no por preferencia guardada — un ciudadano con `city=LIVRAMENTO` ya demuestra el bilingüismo sin campo nuevo. Implementación: **se agrega** `seedDefenseDemoData()` después del dataset genérico existente en `DataLoader`, sin tocarlo (4 `*BrowserTest` loguean con `coopverde`/`juan`/etc. de ese dataset) — 3 orgs nuevas en Rivera (Renacer con perfil completo, Vida Verde, Papel Amigo con perfil reducido a propósito para el caso #8), 5 ciudadanos + 1 guest, 10 solicitudes curadas (una por cada fila pedida) + ~22 `COMPLETED` de volumen con fecha aleatoria en los últimos 6 meses (semilla fija). Nuevo overload de `createRequest` con `trackingCode`/`createdAt` explícitos — los 2 overloads existentes delegan con `null` y mantienen su comportamiento previo intacto. **2 bugs propios encontrados y corregidos verificando el dataset en vivo:** (1) el teléfono del guest #7 se sembró con espacios (`"+598 99 300 001"`) — `RequestService.getGuestRequests` compara por igualdad exacta contra el valor normalizado de la búsqueda, así que nunca hubiera matcheado; corregido a formato ya normalizado. (2) Detectado (y descartado como bug de código) un loop infinito en `/acopio/solicitudes` para **cualquier** cuenta de organización, vieja o nueva — reproducido también con `coopverde` del dataset original intacto, así que no lo causó la siembra; un reinicio limpio del server local lo resolvió, confirmando que era corrupción de estado por los múltiples hot-reloads de devtools acumulados durante la sesión, no un defecto real. Verificado end-to-end contra la instancia local: `/organizaciones?ciudad=RIVERA` lista las 6 orgs (3 originales + 3 nuevas), login como `renacer`/`vidaverde`/`papelamigo` funciona, `/rastrear?telefono=+59899300001&codigo=DEMO2026` encuentra la solicitud de "Beatriz Invitada". Tests: 210/210 no-browser verdes, sin cambio de conteo (cambio es solo de datos de seed, no de lógica de producción) |
| 172 | Bug crítico reportado por el usuario: 100% de las solicitudes de invitado con éxito terminaban redirigiendo a login | Implementado | El más grave de la sesión — confirmado con logs reales de Render que trajo el usuario. Secuencia exacta: `POST /solicitar` crea la solicitud OK (teléfono `92224955` era válido) → redirige a `/rastrear?telefono=+59892224955&codigo=...` → **el `+` sin codificar en el query string se decodifica como espacio** (convención `application/x-www-form-urlencoded` que usan tanto forms como query strings) → `PhoneNumber.normalize` recibe `" 59892224955"`, el `DECORATIVE_CHARS` de #169 limpia el espacio pero el string ya perdió el `+` → falla `PHONE_PATTERN` (exige `+` inicial) → `ValidationException(ERROR_PHONE_INVALID)` sin capturar en `GuestTrackingController.trackGuestForm` → `GlobalExceptionHandler.handleIllegalArgument` intenta volver al `Referer`, pero `SecurityConfig` setea `Referrer-Policy: no-referrer` globalmente — el navegador **nunca** manda ese header, así que ese fallback nunca sirve para esta app — cae a `redirectOrError` → usuario anónimo → `resolveHomeForRole` → `/entrar`. Un invitado (nunca tuvo cuenta) terminaba en la pantalla de login sin ninguna explicación, justo después de crear su solicitud con éxito. Rompía el 100% de los casos porque **todo** teléfono E.164 empieza con `+`, no un edge case de formato. Fix doble: (1) `RequestCreateController` codifica el teléfono con `URLEncoder.encode(...)` antes de armar el redirect — la causa raíz; (2) `RequestService.getGuestRequests` ahora atrapa `IllegalArgumentException` de `PhoneNumber.normalize` y devuelve lista vacía (mismo criterio que ya usa para teléfono/código en blanco) en vez de dejar propagar la excepción — así un invitado que escribe mal el teléfono a mano en `/rastrear` se queda en la página con "sin resultados", nunca rebota a login (pedido explícito del usuario: "por lo menos me tiene que mantener en la página de solicitud de invitado para corregirlo"). Verificado con los logs reales del usuario reproducidos en local (nombre "Fefo", teléfono `92224955`, misma organización) y confirmado en vivo tras reiniciar el server: `/rastrear` responde `200 OK` con la solicitud visible, sin pasar por `/entrar`. 1 test nuevo (`getGuestRequests_malformedPhone_returnsEmptyInsteadOfThrowing`). Tests: 211/211 no-browser verdes (210→211) |
| 173 | Consolidación de `docs/`: triage Canónico/Referencia, 2 solapamientos resueltos (no solo detectados) | Implementado | Pedido explícito del usuario tras discutirlo con su asesor. Se clasificaron los 20 archivos de `docs/` según un criterio único: "¿lo leería un miembro del tribunal?" — no un número de LOC objetivo (~700 era señal, no meta literal; medir contra "sin redundancia, sin desactualizado, una sola fuente de verdad por tema" es el criterio real). **12 Canónicos:** README, INDICE, REQUISITOS, DEFENSA, TRADEOFFS, DIAGRAMAS, ARQUITECTURA, ENDPOINTS, METODOLOGIA, MEJORAS, BOILERPLATE_VS_CORE, USABILIDAD — incluye 4 que una primera pasada rápida había descartado (REQUISITOS, BOILERPLATE_VS_CORE, LIMITACIONES, USABILIDAD): sacarlos para cumplir un número los dejaba peor parados en la defensa que tener más archivos bien organizados. **8 de Referencia** (fuera de la lectura principal, versionados igual, sin borrar): AUDITORIA, COPIES, SEGURIDAD, sincronizacion-codigo-texto.md, gaps-detalle.md, tesis/CONTEXTO_LLM.md (ya estaban ahí) + CORRECCIONES.md y GITFLOW.md (movidos ahora a `docs/referencia/`). **0 obsoletos confirmados** — el contenido sobre blog/Admin/categorías descartadas ya se había limpiado como referencias sueltas en el saneamiento de `DocsContractTest` (#162), no quedó ningún archivo completo para borrar. **2 solapamientos resueltos de verdad, no dejados "a revisar":** (1) `LIMITACIONES.md` (76 LOC) fusionado dentro de `DEFENSA.md` §7 y **borrado** — la pregunta "qué no hiciste" ahora vive en un solo lugar; de paso se actualizó la tabla vieja de §7.1 que decía "sin validación de usabilidad" como trabajo futuro cuando `USABILIDAD.md` (#170) ya existe. (2) `CORRECCIONES.md` (334 LOC, historial fechado 2026-09-11) revisado línea por línea contra `MEJORAS.md`: su contenido de fixes ya está cubierto (con menos detalle en las entradas tempranas) o quedó obsoleto dentro del propio archivo (política de password ya marcada "Superseded #155", SW/PWA descartada, datos de demo desactualizados a "10 usuarios") — no había ningún hecho nuevo que mereciera entrada propia en MEJORAS, salvo su §4 (arquitectura de las 2 fuentes de copies/`I18nCatalogLoader`), que se rescató a `docs/ARQUITECTURA.md` (sección nueva, actualizada con #166) antes de archivar el resto. `GITFLOW.md` reclasificado a Referencia (evidencia de proceso, no argumento de defensa — `METODOLOGIA.md` ya cubre el encuadre DSRM). Ambos movidos con `git mv` (preserva historia) a `docs/referencia/`, `.gitignore` actualizado con las rutas nuevas. Limpieza de referencias rotas: 7 archivos (`REQUISITOS`, `INDICE` ×2, `DEFENSA` ×3, `DataLoader.java`, `README`, `CLAUDE.md`, `DIAGRAMAS.md`) apuntaban a las rutas viejas — todas corregidas. Footer del layout (`base.html`, #168) actualizado: se sacaron los links a los 3 archivos que dejaron de ser canónicos (`CORRECCIONES`/`LIMITACIONES`/`GITFLOW`) y se agregó `USABILIDAD` que faltaba. Resultado: 12 canónicos ≈ 2.560 LOC (no 700 literal, pero sin un solo solapamiento detectado y 0 desactualizado conocido) — cumple el espíritu del pedido. Tests: 211/211 no-browser verdes, sin cambio de conteo (solo reorganización de docs) |
| 174 | Footer del layout: se sacan todos los links a documentación, queda solo "Tesis" | Implementado | Pedido explícito del usuario, después de #168/#173 (que habían completado el footer con los 12 canónicos + visor de diagramas + sandbox). Se revierte a lo mínimo: el footer siempre estuvo marcado como "TEMPORAL... SACAR antes de que haya usuarios reales" (comentario propio del archivo, ver `MEJORAS.md` histórico) — con 0 usuarios reales todavía, el acceso rápido a docs dejó de tener prioridad y se prefiere un footer limpio. Queda un solo link: el documento de tesis (Google Docs externo). No se tocó el backend (`DocsController`, `Routes.SCRATCH_FILE`, `/docs/{file}.md`) — las rutas siguen funcionando para quien las tipee directo, solo se sacó el acceso visual. Verificado en vivo: `footer__links` renderiza un solo `<a>` ("Tesis"). Tests: 211/211 no-browser verdes, sin cambio de conteo (solo template) |
| 175 | Cambiar PIN estando logueado — no existe ningún endpoint | Diferido | Encontrado revisando el menú de un sistema comparable (RESIFPI, IFPI) que tiene "Trocar Senha" como acción dedicada — chequeo contra el código real confirmó que `UserService`/`OrgProfileController` no tienen ningún método que llame `setPassword` fuera del registro inicial (`AuthController:62` solo limpia el campo del form tras el intento, no actualiza nada). Distinto de "recuperación de PIN olvidado" (ya anotado como trabajo futuro en la tesis §6, ese sí requiere canal de verificación tipo SMS) — esto es el caso simple: usuario ya autenticado que quiere cambiar su PIN por cualquier motivo (después de compartir el celular, por precaución, etc.) y hoy no tiene forma de hacerlo. Bajo costo de implementación (mismo patrón que ya existe para actualizar teléfono/ciudad en el perfil, sin infraestructura nueva). Usuario decidió diferirlo explícitamente, no implementar ahora |
| 176 | `UserService.resolveUser` vs `BaseController.getCurrentUser` — mismo lookup, contratos distintos sin documentar | Diferido | Hallazgo de una revisión externa comparando `residuosolido` contra un proyecto ajeno (GeoPicSorter) buscando capas innecesarias — verificado línea por línea contra el código real, no es una capa de más (ninguno de los dos servicios auditados, `RequestService`/`UserService`, tiene abstracción sobrante). El único hallazgo real: `resolveUser(authentication)` tolera anónimo y devuelve `null`; `getCurrentUser(authentication)` (en `BaseController`) asume sesión activa y llama directo a `findAuthenticatedUserByUsername` — revienta si no hay usuario. Ambos terminan en el mismo repository call, pero ninguno de los dos tiene Javadoc que aclare cuándo usar cuál (`getCurrentUser` solo dice "Obtiene el usuario autenticado actual desde la sesión", sin mencionar el caso anónimo; `resolveUser` no tiene Javadoc). No es un bug — cada controller ya usa el que corresponde según si la ruta exige login — pero un desarrollador nuevo podría elegir mal. Fix propuesto (cero riesgo, una línea de Javadoc en cada método): documentar el contrato. Usuario decidió diferirlo |
| 177 | 14 de 238 tests fallan: 5 de 6 clases `*BrowserTest` con selectores Playwright desincronizados de los templates reales | Diferido | `mvn clean test jacoco:report` corrido para auditar cobertura reveló que la suite completa está rota — no es flakiness de entorno, son 4 causas raíz concretas de drift test↔template, ninguna es una regresión de `src/main`: **(1)** `[data-i18n='req_success_title']` — clave sí existe en `es.json`/`pt.json` pero no está en NINGÚN template real; causa 12 de los 14 fallos (`CitizenBrowserTest` ×6, `GuestBrowserTest` ×4, `FullLifecycleBrowserTest` ×2). Esto es exactamente lo que #152 había marcado como sospecha sin poder confirmar ("no se pudo correr para confirmar cuál de las dos" — quedó pendiente porque los `*BrowserTest` necesitan Mongo/Docker, no disponible en ese momento). **(2)** `#hero-title` (selector por ID) en `HomePageBrowserTest.homePageLoads` + `TransversalBrowserTest` ×3 — `public/index.html:9` usa `class="hero__title"`, nunca tuvo ese `id`. **(3)** `[data-i18n='profile_title']` en `CitizenBrowserTest` — la clave sí existe en `es.json` ("Mi perfil") pero el template real de perfil de organización usa `org_profile_title`; no hay pantalla que renderice `profile_title` hoy. **(4)** `HomePageBrowserTest.trackPageLoads` busca `input[id*='phone']` (minúscula) pero `fragments/forms.html` genera `id="trackPhoneNational"` (con "Phone" en mayúscula) — selector CSS case-sensitive, nunca matchea. Impacto directo sobre la defensa: `DEFENSA.md` §4.6/§6 cita "6 clases con Playwright/Chromium real" como evidencia de tests E2E reales — hoy 5 de esas 6 fallan si se corre `mvn clean test`. Fix de las 4 causas es acotado y no toca `src/main` (son selectores de test desactualizados tras refactors ya hechos a propósito: guest ya no tiene pantalla de éxito dedicada, `profile_title` quedó huérfana desde #152). Usuario decidió anotarlo, fix pendiente de decisión |
| 165 | Bug real reportado por el usuario: botones "Registrarse"/"Cerrar sesión" invisibles en el menú hamburguesa mobile | Implementado | El override `.navbar .btn--outline` (texto blanco sobre fondo casi transparente, pensado para la barra oscura del navbar) se aplicaba también a los botones dentro del `.dropdown` mobile — que tiene `background:var(--surface)` **claro**: texto blanco sobre fondo blanco = botón invisible pero clickeable ("los puedo apretar y funciona pero no los veo"). Misma familia de bug que el dropdown de 34px (#142): superficie clara colgando de un contenedor oscuro hereda estilos pensados para el fondo oscuro. Fix: el override se scopeó a `.navbar__actions > .row` — solo los botones directos sobre la barra oscura (Entrar/Registrarse del top-bar) quedan blancos; los del dropdown vuelven a `.btn--outline` base (texto oscuro `--text` + borde `--border`). Cubre ambos casos con una sola regla: "Registrarse" (invitado) y "Cerrar sesión" (logueado) comparten `.dropdown__auth .btn--outline`. Verificado en browser real con viewport mobile 390px: "Registrarse" del dropdown → `color:rgb(27,42,31)` sobre `rgb(255,255,255)`; top-bar → sigue `color:#fff` sobre navbar oscuro. Screenshot del menú abierto confirma ambos botones visibles. Nota: esta clase de bug (cascada CSS por scope demasiado amplio) no la ve ni FrontendCheck ni RenderFragment — solo un browser midiendo `getComputedStyle` — mismo límite documentado en #148/#151 |

- `docs/DEFENSA.md` §7 — limitaciones reconocidas y fuera de alcance (fusionado desde `LIMITACIONES.md`, ver #173).
- `docs/referencia/CORRECCIONES.md` — hardening aplicado y correcciones/limpieza (archivo de referencia, ver #173).
- `docs/AUDITORIA.md` — auditoría de superficies (copies, CSS, esquemas, endpoints, templates).
- `docs/COPIES.md` — single source of truth de textos de UI.
- `docs/SEGURIDAD.md` — checklist de secretos y reglas de `git config`.
